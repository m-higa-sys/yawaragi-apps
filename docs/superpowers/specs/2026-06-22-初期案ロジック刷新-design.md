# 初期案ロジック刷新（実態反映＋制約ベース配置）設計方針

- 作成日: 2026-06-22
- 対象: `shift-create.html`（シフト作成・ケアズ風）の初期案生成ロジック
- ステータス: **設計のみ（実装未着手）**。本番コード未変更。
- 関連残タスク: 伝達ボード（宛先=社長）`db_1782109915796`「初期案ロジック刷新の実装」（終わるまで方式・着手は社長判断）。
- 別件: 春山さんの16時上限ロジック改修（残タスク `db_1782090431958`）は本設計の Phase 3 で合流するが、登録は別。

---

## 0. 背景・確定した事実

### 7/20(月) 看護師ゼロの真因（コードで確認済み）
2026-07-20 は **月曜かつ海の日（祝日）**。看護師は3名で、月曜に出るのは髙山さんだけ。その髙山さんが土日祝休み契約で祝日除外され、春山・石井は月曜が勤務曜日外のため、**看護師ゼロ**になった。

- `initShiftData`（`shift-create.html`）は「曜日パターン反映 → 髙山さんの祝日を強制空欄 → 個別出勤 → 希望休/外せない予定の上書き」だけで組み立て、**人員充足（看護師1+ 等）を配置に一切使っていない**。
- checkAlerts の各チェックは「組み上がった後の事後表示（色付け）」にすぎず、配置にフィードバックされない。これが穴の本質。
- 春山・石井の 7/20 は希望休ではなく「月曜が勤務曜日外」。髙山は祝日休。

### 役割と目標人数（render()フッター・checkAlerts より）
| 役割 | 目標 | メンバー定義（コード実体） |
|---|---|---|
| 看護師 | 1+ | 髙山・春山・石井 |
| 機能訓練指導員 | 2+ | 比嘉（柔整）＋看護師全員（比嘉・髙山・春山・石井） |
| 介護福祉士 | 2+ | 比嘉・星野・勝又・下浦・大久保 |
| 相談員 | 1+ | 星野・勝又・下浦・工藤 |
| フロア | 5+ | 勝又・星野・下浦・工藤・髙山・春山・大久保・石井 |
| 15時以降フロア | 4+ | 同上（比嘉除く） |
| 送迎 | 3+ | 小野・林（専属）＋勝又・星野（兼務） |

連勤ルール: 3連勤禁止＝石井・下浦・春山・髙山／工藤は週3上限（KUDO_LIMIT=3）。

### 人員プール＝全12人（社長＋11スタッフ）
現行 `DEFAULT_PATTERNS` は全員「曜日固定」だが、実態は大久保さんのみ固定で、他は「週N回＋曜日希望＋連勤制約」。曜日固定で持っている土台ズレが、看護師ゼロや全体のちぐはぐの根本原因。さらに**現場に入れる管理者（社長）が人員プールに入っていない**点も補正する。

| スタッフ | 現パターン | 実態（社長確定/推定） |
|---|---|---|
| **社長（管理者）** | （未登録） | **週5・連勤上限なし。役割=相談員/機訓/フロア。看護師不可（資格なし）。管理業務確保のためフル現場固定にしない**（§1a・§確認表参照） |
| 比嘉 | 月〜金 B2 | 週5社員（確定） |
| 勝又 | 月〜金 B2 | 週5社員（確定） |
| 星野 | 月〜金 B2 | 週5社員（確定） |
| 下浦 | 火水金 H | 週3・希望ベース・連勤制約 |
| 工藤 | 月水木 H | 週3(2〜3)（確定） |
| 髙山 | 月水金 E1 | 週3・希望ベース・土日祝休み |
| 春山 | 火木金 E1 | 週3・希望ベース・連勤制約・運転不可 |
| 石井 | 火木 E1 | 週2〜3・希望ベース・連勤制約 |
| 大久保 | 月木 B2 | **曜日固定（唯一）** |
| 小野 | 月〜金 7① | 週4パート（確定・小野寺さん） |
| 林 | 個別指定 | 送迎専属・週2前後・個別指定 |

---

## 1. 勤務ルールのデータモデル案

スタッフ単位で「固定型」「週N回型」「管理者型」を**1つのスキーマで表現**する。

```js
const WORK_RULES = {
  // ① 曜日固定型（大久保さんなど）
  "大久保": {
    type: "fixed",
    days: { 1:"B2", 4:"B2" }        // 曜日(1=月)→勤務パターン
  },

  // ② 週N回＋曜日希望＋連勤上限型（春山・髙山・石井・下浦など）
  "春山": {
    type: "weekly",
    count: 3,                       // 週の出勤回数
    pattern: "E1",                  // 基本の勤務記号（=勤務時間帯）
    prefDays: [2,4,5],              // 希望曜日（優先順・火木金）
    ngDays: [],                     // 出られない曜日
    maxConsecutive: 2,              // 連勤上限（3連勤NGなら2）
    contract: { noWeekend:true, noHolidayWeekday:false },
    timeRule: { extend:"F", maxExtendPerWeek:2, noConsecutiveExtend:true },
    monthOverride: { "2026-07": { timeRule:{ extend:"F", maxExtendPerWeek:2 } } }
  },
  "髙山": { type:"weekly", count:3, pattern:"E1", prefDays:[1,3,5],
            contract:{ noWeekend:true, noHolidayWeekday:true } },  // 土日祝休み

  // ③ 管理者型（社長）= §1a
  "社長": {
    type: "manager",
    count: 5,                       // 週5
    maxConsecutive: null,           // 連勤上限なし
    roles: ["相談員","機訓","フロア"], // 看護師は不可（資格なし）
    pattern: "B2",                  // 現場に入る時間帯（仮）
    adminReserve: true,             // 管理業務(請求/労務/記録)の時間を確保＝フル現場固定にしない
    floorCountPolicy: "managerSafe" // 現場カウントは「管理業務に支障がない範囲」
  }
};
```

### 1a. 管理者（社長）の扱い
- **週5・連勤上限なし**。役割候補は **相談員・機能訓練指導員・介護職員（フロア）**。**看護師は不可**（看護資格なし）。
- ただし**管理業務（請求・労務・記録等）があるため、フル現場固定にはしない**。配置上は「相談員/機訓/フロアの**充足要員候補**」として扱いつつ、管理業務の時間を確保する前提（現場カウントは管理業務に支障がない範囲＝`floorCountPolicy:"managerSafe"`）。
- 用途イメージ: 相談員1+や機訓2+、フロア5+が**他で埋まらない日に社長を充当**する“最後の埋め合わせ要員”。常時フル投入はしない。

ポイント（全型共通）:
- 役割（看護師/介福/相談員/送迎…）は既存定数（NURSE 等）を流用、または各ルールに `roles:[]` を持たせ一元化。
- 希望休・外せない予定（WISH_DATA）／承認済み要望（conditions）は**別レイヤー**として配置時にハード/ソフト制約として流し込む（月ごとに変わるためルール本体に埋めない）。
- 月固有の上書きは `monthOverride: { "2026-07": {...} }`。

### 1b. 兼務ルール（基準準拠）
- **管理者は、管理上支障がない範囲で、相談員・介護職員・機能訓練指導員を兼務可。**
- **看護職員は提供時間帯ずっと専従である必要はない＝必要時配置でよい**（だから看護師1+を「その日1名いればOK」で運用できる）。
- **相談員・機能訓練指導員は他職種と兼務可。**
- ⚠️ **注記: 最終的な兼務可否・常勤換算（FTE）カウントは東松山市の運営基準・指導に従う。本設計の兼務ルールは「自治体確認の上で調整可」とし、確定値は指導内容に合わせて差し替える。**

---

## 2. スタッフ別 勤務ルール確認表（2026-06-22 社長確認で確定）

**🔵 確定（2026-06-22 社長確認）**: 勝又・星野・比嘉＝週5社員／小野（小野寺）＝週4パート／工藤＝週3(2〜3)。連勤上限はTRIPLE_BAN該当(石井・下浦・春山・髙山)=2、それ以外は明示上限なし。希望曜日・役割はコード既知値で確定。**真に未確定なのは下記(e)＝社長の現場投入上限のみ＝Phase 0 には不要・実装Phaseで確認**。

| # | スタッフ | 固定 or 週N回 | 週何回 | 連勤上限 | 希望曜日（優先順） | 出られない曜日 | 主な役割 | 確定 |
|---|---|---|---|---|---|---|---|---|
| 0 | **社長（管理者）** | 管理者(週5) | 5 | なし | （管理業務優先・現場は充足の穴埋め） | ？ | 相談員/機訓/フロア（看護不可） | ✅(現場上限=(e)のみ未確定) |
| 1 | 大久保 | **固定** | － | （なし） | 月・木 | － | 介福/フロア | ✅ |
| 2 | 春山 | 週N回 | 3 | 2(3連勤NG) | 火木金 | （運転不可） | 看護/機訓 | ✅ |
| 3 | 髙山 | 週N回 | 3 | 2(3連勤NG) | 月水金 | 土日祝 | 看護/機訓 | ✅ |
| 4 | 石井 | 週N回 | 2〜3 | 2(3連勤NG) | 火木 | － | 看護/機訓 | ✅ |
| 5 | 下浦 | 週N回 | 3 | 2(3連勤NG) | 火水金 | － | 介福/相談員 | ✅ |
| 6 | 勝又 | **週5(社員)** | 5 | （なし） | 月〜金 | － | 相談/介福/送迎兼務 | ✅ |
| 7 | 星野 | **週5(社員)** | 5 | （なし） | 月〜金 | － | 相談/介福/送迎兼務 | ✅ |
| 8 | 工藤 | **週3** | 2〜3 | （なし・週3上限KUDO_LIMIT） | 月水木 | － | 相談員 | ✅ |
| 9 | 比嘉 | **週5(社員)** | 5 | （なし） | 月〜金 | － | 機訓(柔整)/介福 | ✅ |
| 10 | 小野 | **週4(パート)** | 4 | （なし） | 月〜金から4日 | － | 送迎専属 | ✅ |
| 11 | 林 | 週N回(個別日) | 2前後 | （なし） | 個別指定(INDIVIDUAL_DAYS) | － | 送迎専属 | ✅ |

横断確認項目（(a)〜(d)はコード既知値で確定済み・(e)のみ実装Phaseで確認）:
- (a) 曜日希望の取り込み方：当面は固定希望（毎月同じ）。将来シフト希望入力アプリ連動も可。
- (b) 「週N回」の週の区切り：実装Phaseで月曜始まりに統一予定（月またぎ）。
- (c) 役割の最低人数（看護師1/機訓2/介福2/相談1/フロア5/送迎3/15時4）：現状維持。
- (d) 祝日の特別ルール：髙山（土日祝休み）のみ。
- (e) **【未確定】社長の現場投入上限**：管理業務確保のため「週◯日まで」「◯時間まで」等の目安。Phase 0 には不要。Phase 2（社長を配置候補に入れる段）の前に確認する。

---

## 3. 配置ロジックの方針（チェックを“制約”として使う）

現状の各チェックは「組んだ後に色を付けるだけ＝事後表示」。これを**配置の制約・評価関数に昇格**する。

方針（グリーディ＋制約充足）:
1. 固定型を先に配置（大久保など）。希望休・契約で除外。
2. 週N回型を希望曜日の優先順に仮置きし、各人の `count`・`maxConsecutive`・`ngDays`・希望休を守る。
3. 各日について役割の最低人数を判定（checkAlerts のカウント関数を「評価関数」として再利用）。不足する役割があれば、その役割を満たせる週N回型スタッフの未消化枠を、希望曜日を崩してでもその日へ寄せる（例：月曜に看護師→看護師の週N枠を月曜へ）。
4. それでも埋まらない相談員/機訓/フロアは、**管理者（社長）を `floorCountPolicy:"managerSafe"` の範囲で充当**（最後の埋め合わせ）。看護師の穴は社長では埋められない（資格なし）ので穴のまま明示。
5. 解けたら確定、解けなければ穴として残す（§4）。

実装の肝: **checkAlerts の集計ロジック（role→count）を純粋関数に切り出し、「配置中の評価」と「事後表示」で共用**する。表示と配置の判定基準を必ず一致させ、二重実装を避ける。

---

## 4. 制約が両立しないときの優先順位・穴の扱い

ハード制約（絶対に破らない）:
- 希望休／外せない予定、契約（髙山＝土日祝休）、運転不可、連勤上限、週N回の上限、**資格（社長は看護不可）**。

ソフト制約（目標。両立しなければ緩める）。緩める順序の提案:
1. 看護師1+（人員基準・最優先で死守。社長では埋められない＝看護師の週N枠/希望曜日を最優先で寄せる）
2. 送迎3+ / フロア5+（運営の安全）
3. 機訓2+ / 介福2+ / 相談1+ / 15時以降4+（サービスの質。社長を管理業務に支障ない範囲で充当）
4. 本人の曜日希望（最後に妥協）

埋まらない日の扱い:
- 自動で人を捏造補充しない。穴は「🔴 7/20(月) 看護師ゼロ」のように明示し `#alertBox` に表示。
- あわせて「誰を入れれば埋まるか」の候補を提示（例：7/20＝春山 or 石井を月曜に追加／相談員不足なら社長を充当）。最終判断は社長。

---

## 5. 実装の段階分け（最小から・各段の範囲とリスク）

| 段階 | 内容 | 変更範囲 | リスク | 検証 |
|---|---|---|---|---|
| **Phase 0** | `DEFAULT_PATTERNS`→`WORK_RULES`へデータ構造だけ置換。fixed型のみ実装し**現状を完全再現**（週N回型も当面 prefDays をそのまま固定展開／社長は当面プールに「候補」として定義のみ・自動配置はしない） | initShiftData＋定数のみ | 低（出力不変が目標） | 既存月（5/6/7月）の初期案が変更前と一致することをNodeで実測 |
| **Phase 1** | weekly型の自動割当（希望曜日優先・連勤上限・希望休）。役割最低人数はまだ事後警告のまま、穴は穴表示 | initShiftData配置ロジック | 中（割当結果が変わる） | 役割充足の件数比較・連勤違反ゼロを検証 |
| **Phase 2** | 役割最低人数を配置制約に昇格（不足役割へ週N枠を寄せる→最後に社長を管理業務に支障ない範囲で充当）＋優先順位適用。checkAlerts集計を純関数化して共用。**社長の現場投入上限(e)はこの段の前に確認** | 配置ロジック＋checkAlertsリファクタ | 中〜高（最適化の挙動） | 7/20のような祝日×月曜で看護師が自動充足 or 候補提示／相談員不足で社長充当が働くか |
| **Phase 3** | 希望休API・承認済み要望（週N回・16時連勤回避）を制約に統合。春山16時ルール本実装（残タスク db_1782090431958 と合流） | API連携＋制約 | 高（外部データ依存） | 実データで要望反映を実測 |

進め方の推奨: まず Phase 0（出力不変のリファクタ）だけを安全に入れ、回帰OKを確認してから Phase 1 以降へ。各段で本番pushの前にNode検証＋社長確認を挟む。

---

## 6. 再開時の入口（次にやること）
1. ~~§2 の確認表を社長に埋めてもらう~~ → **2026-06-22 確定済み**（残る未確定は(e)社長の現場投入上限のみ＝Phase 2 直前に確認）。
2. データモデル（§1）・兼務ルール（§1b）・優先順位（§4）の方向性に社長OK。兼務/常勤換算は東松山市の運営基準・指導で要確定。
3. **Phase 0（§7）の実装**へ。`WORK_RULES` 全12人分の初期値＋回帰テスト計画は §7 に確定済み。
4. 本番コードは各Phaseとも Node検証＋社長確認後に push。

---

## 7. Phase 0 詳細設計（出力不変リファクタ）

**目的**: `DEFAULT_PATTERNS`(曜日固定)を `WORK_RULES`(将来は固定/週N回/管理者を表現)に置換するが、**Phase 0 では出力を1セルも変えない**。土台だけ入れ替え、回帰テストで同一を担保する。

### 7.1 WORK_RULES 全12人の初期値（Phase 0＝現状完全再現）
Phase 0 は全員 `fixed` 展開（現 `DEFAULT_PATTERNS` と1:1）。実態(週N回)は `_actual` メモに残すだけで配置には使わない（Phase 1 で `type:"weekly"` に切替）。社長は **定義のみ・自動配置しない**。

```js
const WORK_RULES = {
  // 週5社員（月〜金固定）
  "比嘉": { type:"fixed", days:{1:"B2",2:"B2",3:"B2",4:"B2",5:"B2"}, roles:["機訓","介福"], _actual:"週5社員" },
  "勝又": { type:"fixed", days:{1:"B2",2:"B2",3:"B2",4:"B2",5:"B2"}, roles:["相談","介福","送迎兼務"], _actual:"週5社員" },
  "星野": { type:"fixed", days:{1:"B2",2:"B2",3:"B2",4:"B2",5:"B2"}, roles:["相談","介福","送迎兼務"], _actual:"週5社員" },
  // 週N回（Phase0は現状の曜日で固定展開・実態はweekly）
  "下浦": { type:"fixed", days:{2:"H",3:"H",5:"H"}, roles:["介福","相談"], _actual:"週3・希望ベース・3連勤NG" },
  "工藤": { type:"fixed", days:{1:"H",3:"H",4:"H"}, roles:["相談"], _actual:"週3(2〜3)" },
  "髙山": { type:"fixed", days:{1:"E1",3:"E1",5:"E1"}, roles:["看護","機訓"], contract:{noWeekend:true,noHolidayWeekday:true}, _actual:"週3・土日祝休み" },
  "春山": { type:"fixed", days:{2:"E1",4:"E1",5:"E1"}, roles:["看護","機訓"], _actual:"週3・運転不可・3連勤NG・16時延長候補" },
  "石井": { type:"fixed", days:{2:"E1",4:"E1"}, roles:["看護","機訓"], _actual:"週2〜3・3連勤NG" },
  // 曜日固定（唯一の真の固定）
  "大久保": { type:"fixed", days:{1:"B2",4:"B2"}, roles:["介福","フロア"], _actual:"曜日固定" },
  // 送迎専属
  "小野": { type:"fixed", days:{1:"7①",2:"7①",3:"7①",4:"7①",5:"7①"}, roles:["送迎"], _actual:"週4パート(Phase1でcount4)" },
  "林":   { type:"individual", roles:["送迎"], _actual:"週2前後・個別指定(INDIVIDUAL_DAYS)" }, // Phase0は既存INDIVIDUAL_DAYSをそのまま使用
  // 管理者（Phase0は定義のみ・配置しない）
  "社長": { type:"manager", count:5, maxConsecutive:null, roles:["相談","機訓","フロア"], pattern:"B2", adminReserve:true, floorCountPolicy:"managerSafe", _phase0:"定義のみ・自動配置なし" }
};
```

### 7.2 展開関数（現 initShiftData のパターン取得を置換）
`initShiftData` 内の `const pattern = DEFAULT_PATTERNS[s]?.[dowNum];` を以下の `rulePatternForDay()` に差し替える。**他の処理（髙山祝日の強制空欄／INDIVIDUAL_DAYS／希望休・外せない予定の上書き順）は一切変更しない**。

```js
// Phase0: 現DEFAULT_PATTERNS と同一の結果を返す（fixed=days[dow] / individual=なし(林はINDIVIDUAL_DAYSで処理) / manager=配置しない）
function rulePatternForDay(name, dowNum) {
  const r = WORK_RULES[name];
  if (!r) return undefined;
  if (r.type === "fixed")      return r.days ? r.days[dowNum] : undefined;
  if (r.type === "individual") return undefined; // 林は従来どおりINDIVIDUAL_DAYSで個別反映
  if (r.type === "manager")    return undefined; // Phase0は社長を自動配置しない
  return undefined;
}
```
- 社長は STAFF_KEY に**まだ追加しない**（Phase 0 は人数・出力を変えないため）。WORK_RULES に定義だけ置く。社長を実際の配置候補に入れるのは Phase 2。

### 7.3 回帰テスト計画（出力不変の担保）
- **方式**: `initShiftData` の shiftData 生成部は DOM 非依存。これを純関数として切り出し、**現行版(DEFAULT_PATTERNS)と新版(WORK_RULES)で同一入力→出力をセル単位で全比較**する Node テスト `scripts/test-workrules-phase0.js` を用意。
- **対象**: 2026-05 / 2026-06 / 2026-07 の各月 × `blankMode` false/true。各月の `WISH_DATA`(コード内既存値)・`INDIVIDUAL_DAYS`・`HOLIDAYS` を使用。
- **比較項目**: 全スタッフ(11名) × 全日 の勤務記号。加えて (a)髙山の祝日強制空欄、(b)林のINDIVIDUAL_DAYS反映、(c)希望休→「休」上書き、(d)外せない予定→「休」上書き、の各結果。
- **合格基準**: **全セル diff = 0**（`JSON.stringify`一致）。1セルでも差異があれば Phase 0 は不合格としてマージしない。
- **副次**: render / checkAlerts / 保存 / API は触らないので対象外（呼び出し経路が変わらないことをコードレビューで確認）。

### 7.4 変更範囲・リスク・ロールバック
- **変更**: ①`WORK_RULES` 定数を追加 ②`rulePatternForDay()` を追加 ③`initShiftData` のパターン取得1行を差し替え。`DEFAULT_PATTERNS` は当面**残置**（Phase 1 で削除）。
- **不変**: `PATTERNS` / `INDIVIDUAL_DAYS` / 髙山祝日ロジック / 希望休・外せない予定の上書き / render / checkAlerts / 保存 / クラウド同期 / API。
- **リスク**: 低（出力不変が目標・回帰テストで担保）。
- **ロールバック**: 1コミットで戻せる（定数追加＋1行差し替えのみ）。

### 7.5 Phase 0 完了の定義
1. `scripts/test-workrules-phase0.js` が3か月×2モードで全セル一致(diff=0)。
2. 既存の手組み保存(localStorage)・クラウド復元の挙動が不変（コードレビュー）。
3. 社長が実機でシフト作成を開き、初期案が従来どおりであることを目視確認。
→ ここまで満たしたら Phase 1（weekly型の自動割当）の詳細設計に進む。
