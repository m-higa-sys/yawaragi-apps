# 加算・事業所情報アプリ kasan.html 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** やわらぎが取得している加算13件と事業所基本情報を、スタッフが1タップで見られる独立アプリ `kasan.html` を作る（ケアマネ対応用にサービスコードを併記）。

**Architecture:** 正本＝利用者台帳SS の新規シート `kasan_master`（section判別列で4カテゴリを1シート）。GAS は `action=kasan` で読み取り専用の **JSONP** を返す（board GAS は GET=JSONP）。判定・整形は `kasan-core.js` の純関数に隔離し node でテストする。フロントは版ゲート自己完結の単一HTML。認証は版ゲートのみ（設計書 §2.1）。

**Tech Stack:** Google Apps Script（clasp）／素の JS（フレームワーク無し）／node 直実行テスト（jsdom は**未導入なので使わない**）

**設計書:** `docs/superpowers/specs/2026-07-17-kasan-app-design.md`（承認済み）

---

## 前提知識（この計画を実行する人が知らないこと）

- **board GAS は GET=JSONP・POST=no-cors。** `fetch(GAS+'?action=kasan')` では**動かない**。script要素注入＋`callback=` 必須。サーバは既存ヘルパ `respond(data, callback)`（`gas/yawaragi-board/コード.js:6694`）を使う。
- **`*-core.js` は `require()` を持ってはいけない。** clasp の `rootDir="."` で core も GAS へ push されるため、`require` があると本番ロード時に停止する。export は末尾の `if (typeof module !== 'undefined' && module.exports)` ガード内のみ。
- **core に定義し、コード.js は呼ぶだけ。** コード.js に同じ関数を内包すると二重宣言で GAS が停止する。
- **Sheets の型強制トラップ3種**（設計書 §5.2）。`781241` と `1173300995` は数値化され、日付は Date 化されて **+16h ずれる**（シートTZ=米西海岸／スクリプトTZ=東京）。→ **全列テキスト書式＋`setValues`**。`appendRow` は使わない。
- **版ゲートの正本は `genba.html`。** `session-board.html` とは byte 完全一致（889バイト・実測済み）。テストは byte 比較で検証する。
- **本番書込（clasp push / deploy / git push）は全て社長の手。** 実装者は直前で止まる。

## File Structure

| ファイル | 責務 |
|---|---|
| `gas/yawaragi-board/kasan-core.js`（新規） | 純関数のみ。シート2次元配列の解釈・正規化・並び順・グルーピング。GAS API 非依存 |
| `scripts/test-kasan-core.js`（新規） | 上記の node 直実行テスト |
| `gas/yawaragi-board/コード.js`（改修・**additive-only**） | `KASAN_SHEET` / `KASAN_MASTER_SEED` / `setupKasanMaster` / `kasan(e)` / doGet 分岐 |
| `kasan.html`（新規・repo直下） | 版ゲート＋JSONP取得＋カード4枚描画 |
| `scripts/test-kasan-version-gate.js`（新規） | 版ゲートが genba.html と byte 一致することを実コード抽出で検証 |
| `scripts/test-kasan-html.js`（新規） | kasan.html の静的検証（JSONP方式・esc網羅・未取得の警告表示） |

---

## Task 0: worktree 準備

**Files:** なし（環境構築のみ）

- [ ] **Step 1: 最新の origin/master を取得**

```bash
cd /c/dev/yawaragi-apps && git fetch origin && git rev-parse --abbrev-ref HEAD
```

Expected: `master`（本体は master 固定。ここで `git checkout` してはならない＝2026-07-13「ブランチ回転台」事故の再発防止）

- [ ] **Step 2: 専用worktreeを最新 origin/master から切る**

```bash
git worktree add C:/tmp/wt-kasan -b feat/kasan-app origin/master
```

Expected: `Preparing worktree (new branch 'feat/kasan-app')`

- [ ] **Step 3: 起点が origin/master と一致することを確認（先祖返り防止）**

```bash
cd /c/tmp/wt-kasan && git rev-parse HEAD && git rev-parse origin/master
```

Expected: 2つのSHAが一致

---

## Task 1: kasan-core.js — コード正規化と冪等キー

**Files:**
- Create: `C:/tmp/wt-kasan/gas/yawaragi-board/kasan-core.js`
- Test: `C:/tmp/wt-kasan/scripts/test-kasan-core.js`

- [ ] **Step 1: 失敗するテストを書く**

`scripts/test-kasan-core.js`:

```js
// 加算・事業所情報アプリ kasan.html 純関数テスト
// 実行: node scripts/test-kasan-core.js
const path = require('path');
const c = require(path.join(__dirname, '..', 'gas', 'yawaragi-board', 'kasan-core.js'));
let pass = 0, fail = 0;
function eq(a, e, m){ const A=JSON.stringify(a),E=JSON.stringify(e); if(A===E){pass++;console.log('  PASS '+m);}else{fail++;console.log('  FAIL '+m+'\n    exp '+E+'\n    act '+A);} }
function ok(cnd, m){ if(cnd){pass++;console.log('  PASS '+m);}else{fail++;console.log('  FAIL '+m);} }

console.log('[定数]');
eq(c.KASAN_HEADER, ['section','表示順','系統','コード','項目','値','最終確認日','備考'], 'ヘッダ8列');
eq(c.KASAN_SECTIONS, ['基本情報','運営体制','地域区分','加算'], 'section4種');
eq(c.KASAN_KEITOU, ['介護給付','総合事業'], '系統2種');

console.log('\n[kasanNormalizeCode] ★Sheetsが781241を数値化する罠');
ok(c.kasanNormalizeCode(781241) === '781241', '数値781241→文字列(指数・小数を作らない)');
ok(c.kasanNormalizeCode(781241.0) === '781241', '小数表現でも781241');
ok(c.kasanNormalizeCode('A61111') === 'A61111', '英字混在は素通し');
ok(c.kasanNormalizeCode('  786108  ') === '786108', '前後空白をtrim');
ok(c.kasanNormalizeCode(null) === '', 'null→空');
ok(c.kasanNormalizeCode(undefined) === '', 'undefined→空');
ok(c.kasanNormalizeCode('') === '', '空→空');

console.log('\n[kasanSeedKey_] section|コード|項目 で冪等');
ok(c.kasanSeedKey_(['加算',10,'介護給付','781241','本体（地域通所介護11）','','2026-07-17','']) === '加算|781241|本体（地域通所介護11）', '加算行のキー');
ok(c.kasanSeedKey_(['基本情報',10,'','','法人名','株式会社キープフィットライフ','2026-07-17','']) === '基本情報||法人名', '基本情報は項目で効く');
ok(c.kasanSeedKey_(['加算',10,'介護給付',781241,'本体','','','']) === c.kasanSeedKey_(['加算',10,'介護給付','781241','本体','','','']), '★数値/文字列のコードで同一キー＝再実行で重複しない');
ok(c.kasanSeedKey_(null) === '', 'null→空');
```

- [ ] **Step 2: テストを走らせて失敗を確認**

Run: `cd /c/tmp/wt-kasan && node scripts/test-kasan-core.js`
Expected: FAIL — `Cannot find module ...kasan-core.js`

- [ ] **Step 3: 最小実装を書く**

`gas/yawaragi-board/kasan-core.js`:

```js
// 加算・事業所情報アプリ kasan.html の純関数（2026-07-17）
// 設計書: docs/superpowers/specs/2026-07-17-kasan-app-design.md
// テスト: scripts/test-kasan-core.js ／ 呼び出し元: コード.js（action=kasan / setupKasanMaster）
//
// GAS/node 両用（session-board-core.js と同じ流儀）。SpreadsheetApp 等の GAS API に依存しない。
// ※require() は持たない（GAS本番でロード時に停止しない・他 *-core.js と同方式）。
//
// 罠: Sheets は 781241 を数値化する（A61111 は英字混在なので文字列のまま）。
//     kasan_master は全列テキスト書式で作るが、書式適用前の行や手貼りに備え
//     kasanNormalizeCode で二重に守る。

var KASAN_HEADER = ['section', '表示順', '系統', 'コード', '項目', '値', '最終確認日', '備考'];
var KASAN_SECTIONS = ['基本情報', '運営体制', '地域区分', '加算'];
var KASAN_KEITOU = ['介護給付', '総合事業'];

// コード正規化。数値化された 781241 を文字列へ戻す。A61111 等は素通し。
function kasanNormalizeCode(v) {
  if (v === null || v === undefined) return '';
  if (typeof v === 'number') return String(Math.round(v));
  return String(v).trim();
}

// シード行 [section,表示順,系統,コード,項目,値,最終確認日,備考] の冪等キー。
// section|コード|項目 で一意（加算はコードで、基本情報等は項目で効く）。
function kasanSeedKey_(row) {
  if (!row) return '';
  return [String(row[0] == null ? '' : row[0]).trim(),
          kasanNormalizeCode(row[3]),
          String(row[4] == null ? '' : row[4]).trim()].join('|');
}

if (typeof module !== 'undefined' && module.exports) {
  module.exports = {
    KASAN_HEADER: KASAN_HEADER,
    KASAN_SECTIONS: KASAN_SECTIONS,
    KASAN_KEITOU: KASAN_KEITOU,
    kasanNormalizeCode: kasanNormalizeCode,
    kasanSeedKey_: kasanSeedKey_
  };
}
```

- [ ] **Step 4: テストを走らせて通ることを確認**

Run: `cd /c/tmp/wt-kasan && node scripts/test-kasan-core.js`
Expected: `===== 14 passed / 0 failed =====`（exit 0）

- [ ] **Step 5: コミット**

```bash
cd /c/tmp/wt-kasan && git add gas/yawaragi-board/kasan-core.js scripts/test-kasan-core.js && git commit -m "feat(kasan): 純関数 kasanNormalizeCode/kasanSeedKey_（781241の数値化罠に対応・TDD）"
```

---

## Task 2: kasan-core.js — kasanParseRows（ヘッダ名で列解決）

**Files:**
- Modify: `C:/tmp/wt-kasan/gas/yawaragi-board/kasan-core.js`
- Test: `C:/tmp/wt-kasan/scripts/test-kasan-core.js`

- [ ] **Step 1: 失敗するテストを追記**

`scripts/test-kasan-core.js` の末尾（`process.exit` 行より前）に追記:

```js
console.log('\n[kasanParseRows] ヘッダ「名」で列解決＝列順の入替に強い');
const H = ['section','表示順','系統','コード','項目','値','最終確認日','備考'];
eq(c.kasanParseRows([H, ['加算', 10, '介護給付', 781241, '本体（地域通所介護11）', '', '2026-07-17', '']]),
   [{section:'加算', order:10, keitou:'介護給付', code:'781241', item:'本体（地域通所介護11）', value:'', checkedAt:'2026-07-17', note:''}],
   '基本の1行（コードは数値でも文字列化）');

// 列順を入れ替えても同じ結果になること＝位置決め打ちでない証明
const H2 = ['備考','項目','section','コード','系統','値','最終確認日','表示順'];
eq(c.kasanParseRows([H2, ['', '本体（地域通所介護11）', '加算', 781241, '介護給付', '', '2026-07-17', 10]]),
   [{section:'加算', order:10, keitou:'介護給付', code:'781241', item:'本体（地域通所介護11）', value:'', checkedAt:'2026-07-17', note:''}],
   '★列順を入れ替えても同一結果');

eq(c.kasanParseRows([]), [], '空→[]');
eq(c.kasanParseRows([H]), [], 'ヘッダのみ→[]');
eq(c.kasanParseRows(null), [], 'null→[]');
eq(c.kasanParseRows([H, ['', '', '', '', '', '', '', '']]), [], 'section空の行はスキップ');
eq(c.kasanParseRows([H, ['基本情報', '', '', '', '法人名', '株式会社キープフィットライフ', '', '']])[0].order, 9999,
   '表示順が空→9999（末尾へ）');
eq(c.kasanParseRows([H, ['基本情報', 'あ', '', '', '法人名', 'X', '', '']])[0].order, 9999,
   '表示順が非数値→9999（末尾へ）');
// ヘッダに存在しない列を要求しても落ちない
eq(c.kasanParseRows([['section','項目'], ['基本情報', '法人名']]),
   [{section:'基本情報', order:9999, keitou:'', code:'', item:'法人名', value:'', checkedAt:'', note:''}],
   '★列が欠けていても落ちず空で埋まる');
// 行が短くても落ちない
eq(c.kasanParseRows([H, ['基本情報']])[0].item, '', '行が短くても落ちない');

console.log('\n[kasanParseRows] ★最終確認日のTZずれ防止（Date化されても+16hずらさない）');
eq(c.kasanParseRows([H, ['加算', 10, '介護給付', '781241', '本体', '', new Date(2026, 6, 17), '']])[0].checkedAt,
   '2026-07-17', 'Dateはローカル年月日で yyyy-MM-dd 化（UTC変換しない＝日付がずれない）');
```

- [ ] **Step 2: テストを走らせて失敗を確認**

Run: `cd /c/tmp/wt-kasan && node scripts/test-kasan-core.js`
Expected: FAIL — `c.kasanParseRows is not a function`

- [ ] **Step 3: 実装を追記**

`kasan-core.js` の `kasanSeedKey_` の下、export ブロックより前に追記:

```js
// 表示順。数値化できない（空・文字）なら末尾へ落とす。
function kasanOrderNum_(v) {
  var n = parseInt(v, 10);
  return isNaN(n) ? 9999 : n;
}

// 最終確認日。全列テキスト書式なので通常は文字列で来るが、書式適用前の行が Date で
// 来た場合に備える。★toISOString を使わない＝UTC変換で日付が1日ずれるため
// （シートTZ=米西海岸／スクリプトTZ=東京の +16h ずれと同種の罠）。ローカル年月日で組む。
function kasanFormatDate_(v) {
  if (!v) return '';
  if (v instanceof Date) {
    var m = v.getMonth() + 1, d = v.getDate();
    return v.getFullYear() + '-' + (m < 10 ? '0' : '') + m + '-' + (d < 10 ? '0' : '') + d;
  }
  return String(v).trim();
}

// シート2次元配列 → オブジェクト配列。ヘッダ「名」で列を解決する（列順の入替・列追加に強い）。
function kasanParseRows(values) {
  if (!values || values.length < 2) return [];
  var header = (values[0] || []).map(function (h) { return String(h == null ? '' : h).trim(); });
  var idx = {};
  KASAN_HEADER.forEach(function (name) { idx[name] = header.indexOf(name); });
  var out = [];
  for (var i = 1; i < values.length; i++) {
    var r = values[i] || [];
    var cell = function (name) {
      var j = idx[name];
      if (j < 0 || j >= r.length) return '';
      return r[j] == null ? '' : r[j];
    };
    var section = String(cell('section')).trim();
    if (!section) continue;  // 空行スキップ
    out.push({
      section: section,
      order: kasanOrderNum_(cell('表示順')),
      keitou: String(cell('系統')).trim(),
      code: kasanNormalizeCode(cell('コード')),
      item: String(cell('項目')).trim(),
      value: String(cell('値')).trim(),
      checkedAt: kasanFormatDate_(cell('最終確認日')),
      note: String(cell('備考')).trim()
    });
  }
  return out;
}
```

export ブロックに追記:

```js
    kasanOrderNum_: kasanOrderNum_,
    kasanFormatDate_: kasanFormatDate_,
    kasanParseRows: kasanParseRows,
```

- [ ] **Step 4: テストを走らせて通ることを確認**

Run: `cd /c/tmp/wt-kasan && node scripts/test-kasan-core.js`
Expected: `===== 25 passed / 0 failed =====`

- [ ] **Step 5: コミット**

```bash
cd /c/tmp/wt-kasan && git add gas/yawaragi-board/kasan-core.js scripts/test-kasan-core.js && git commit -m "feat(kasan): kasanParseRows（ヘッダ名で列解決・DateのTZずれ防止）"
```

---

## Task 3: kasan-core.js — kasanSortRows（表示順→コードの安定ソート）

**Files:**
- Modify: `C:/tmp/wt-kasan/gas/yawaragi-board/kasan-core.js`
- Test: `C:/tmp/wt-kasan/scripts/test-kasan-core.js`

- [ ] **Step 1: 失敗するテストを追記**

`scripts/test-kasan-core.js` の末尾（`process.exit` 行より前）に追記:

```js
console.log('\n[kasanSortRows] 表示順→コード の安定ソート・非破壊');
const rs = [
  {section:'加算', order:20, code:'785053', item:'個訓Ⅰ2'},
  {section:'加算', order:10, code:'781241', item:'本体'},
  {section:'加算', order:9999, code:'999999', item:'表示順なし'},
  {section:'加算', order:10, code:'A61111', item:'総合本体'}
];
eq(c.kasanSortRows(rs).map(function(x){return x.code;}), ['781241','A61111','785053','999999'],
   '表示順昇順／同値はコード順（781241 < A61111）／9999は末尾');
const before = rs.map(function(x){return x.code;});
c.kasanSortRows(rs);
eq(rs.map(function(x){return x.code;}), before, '★非破壊（引数の配列を並べ替えない）');
eq(c.kasanSortRows([]), [], '空→[]');
eq(c.kasanSortRows(null), [], 'null→[]');
```

- [ ] **Step 2: テストを走らせて失敗を確認**

Run: `cd /c/tmp/wt-kasan && node scripts/test-kasan-core.js`
Expected: FAIL — `c.kasanSortRows is not a function`

- [ ] **Step 3: 実装を追記**

`kasan-core.js` の `kasanParseRows` の下に追記:

```js
// 表示順 → コード の安定ソート（非破壊＝呼び出し側の配列を壊さない）。
function kasanSortRows(rows) {
  return (rows || []).slice().sort(function (a, b) {
    if (a.order !== b.order) return a.order - b.order;
    return a.code < b.code ? -1 : (a.code > b.code ? 1 : 0);
  });
}
```

export ブロックに追記:

```js
    kasanSortRows: kasanSortRows,
```

- [ ] **Step 4: テストを走らせて通ることを確認**

Run: `cd /c/tmp/wt-kasan && node scripts/test-kasan-core.js`
Expected: `===== 29 passed / 0 failed =====`

- [ ] **Step 5: コミット**

```bash
cd /c/tmp/wt-kasan && git add gas/yawaragi-board/kasan-core.js scripts/test-kasan-core.js && git commit -m "feat(kasan): kasanSortRows（表示順→コードの安定ソート・非破壊）"
```

---

## Task 4: kasan-core.js — グルーピング2種（未知データを黙って捨てない）

**Files:**
- Modify: `C:/tmp/wt-kasan/gas/yawaragi-board/kasan-core.js`
- Test: `C:/tmp/wt-kasan/scripts/test-kasan-core.js`

- [ ] **Step 1: 失敗するテストを追記**

`scripts/test-kasan-core.js` の末尾（`process.exit` 行より前）に追記:

```js
console.log('\n[kasanGroupBySection] 未知sectionを黙って捨てない');
const g = c.kasanGroupBySection([
  {section:'基本情報', code:'', item:'法人名'},
  {section:'加算', code:'781241', item:'本体'},
  {section:'謎', code:'', item:'知らないsection'}
]);
eq(g['基本情報'].map(function(x){return x.item;}), ['法人名'], '基本情報に振る');
eq(g['加算'].map(function(x){return x.code;}), ['781241'], '加算に振る');
eq(g['不明'].map(function(x){return x.item;}), ['知らないsection'], '★未知sectionは「不明」へ＝消えない');
eq(g['運営体制'], [], '該当なしは空配列');
eq(c.kasanGroupBySection([]), {'基本情報':[],'運営体制':[],'地域区分':[],'加算':[],'不明':[]}, '空→全カテゴリ空');
eq(c.kasanGroupBySection(null), {'基本情報':[],'運営体制':[],'地域区分':[],'加算':[],'不明':[]}, 'null→全カテゴリ空');
// ★プロトタイプ汚染耐性: section が 'constructor' でも落ちない
ok((function(){ try { c.kasanGroupBySection([{section:'constructor', code:'', item:'x'}]); return true; } catch(e){ return false; } })(),
   "★section='constructor' でも例外にならない");
eq(c.kasanGroupBySection([{section:'constructor', code:'', item:'x'}])['不明'].length, 1,
   "★section='constructor' は「不明」へ");

console.log('\n[kasanSplitKeitou] 系統が空の加算行も落とさない');
const sp = c.kasanSplitKeitou([
  {keitou:'介護給付', code:'781241'},
  {keitou:'総合事業', code:'A61111'},
  {keitou:'', code:'999999'}
]);
eq(sp['介護給付'].map(function(x){return x.code;}), ['781241'], '介護給付');
eq(sp['総合事業'].map(function(x){return x.code;}), ['A61111'], '総合事業');
eq(sp['系統不明'].map(function(x){return x.code;}), ['999999'], '★系統が空→「系統不明」へ＝沈黙させない');
eq(c.kasanSplitKeitou([]), {'介護給付':[],'総合事業':[],'系統不明':[]}, '空→全系統空');
eq(c.kasanSplitKeitou(null), {'介護給付':[],'総合事業':[],'系統不明':[]}, 'null→全系統空');
```

- [ ] **Step 2: テストを走らせて失敗を確認**

Run: `cd /c/tmp/wt-kasan && node scripts/test-kasan-core.js`
Expected: FAIL — `c.kasanGroupBySection is not a function`

- [ ] **Step 3: 実装を追記**

`kasan-core.js` の `kasanSortRows` の下に追記:

```js
// section 別に束ねる。未知 section は「不明」へ入れる＝黙って捨てない
// （シートに想定外の値が入ったとき、画面から消えるより見えた方が直せる）。
// ★ KASAN_SECTIONS.indexOf で判定する。out[s] の存在確認だと section='constructor' 等で
//    Object.prototype 由来の値を掴んで .push が例外になる。
function kasanGroupBySection(rows) {
  var out = { '基本情報': [], '運営体制': [], '地域区分': [], '加算': [], '不明': [] };
  (rows || []).forEach(function (r) {
    var s = (r && r.section) || '';
    if (KASAN_SECTIONS.indexOf(s) >= 0) out[s].push(r);
    else out['不明'].push(r);
  });
  return out;
}

// 加算行を系統で分ける。系統が空/未知の行は「系統不明」へ入れる＝落とさない。
function kasanSplitKeitou(rows) {
  var out = { '介護給付': [], '総合事業': [], '系統不明': [] };
  (rows || []).forEach(function (r) {
    var k = (r && r.keitou) || '';
    if (KASAN_KEITOU.indexOf(k) >= 0) out[k].push(r);
    else out['系統不明'].push(r);
  });
  return out;
}
```

export ブロックに追記:

```js
    kasanGroupBySection: kasanGroupBySection,
    kasanSplitKeitou: kasanSplitKeitou
```

- [ ] **Step 4: テストを走らせて通ることを確認**

Run: `cd /c/tmp/wt-kasan && node scripts/test-kasan-core.js`
Expected: `===== 42 passed / 0 failed =====`

- [ ] **Step 5: コミット**

```bash
cd /c/tmp/wt-kasan && git add gas/yawaragi-board/kasan-core.js scripts/test-kasan-core.js && git commit -m "feat(kasan): kasanGroupBySection/kasanSplitKeitou（未知sectionと系統空を黙って捨てない）"
```

---

## Task 5: GAS 結線（additive-only）

**Files:**
- Modify: `C:/tmp/wt-kasan/gas/yawaragi-board/コード.js`（**追記のみ・既存行を1行も変えない**）

- [ ] **Step 1: 本番GASと突合（★既知地雷の確認）**

```bash
cd /c/tmp/wt-kasan/gas/yawaragi-board && clasp pull
cd /c/tmp/wt-kasan && git diff --stat
```

Expected: `コード.js` に本番側のドリフトが出る。**この差分を捨ててはならない。**
既知のドリフト（memory記録済み）:
- `コード.js` 6003-6004 付近の**平文LINEトークン**（本番のみ・repoは PropertiesService 化済み）
- `DENGON_MIGRATE_SEED` の伝達ボード2件（本番のみ）

★ドリフトを消すと本番のLINE通知か伝達ボードが壊れる。**本タスクは追記だけを行い、pull で出た差分はそのまま残す。**
※Bash がサンドボックスだと clasp が隔離されて失敗する（memory: `dangerouslyDisableSandbox` 必須）。

- [ ] **Step 2: シート定義とSEEDを追記**

`コード.js` の末尾に追記（`KASAN_HEADER` は `kasan-core.js` が持つので**ここで再定義しない**＝二重宣言でGASが停止する）:

```js
// =============================================================
// 加算・事業所情報（2026-07-17）
//   ?action=kasan&callback=cb  … kasan_master 読み取り専用（JSONP）
//   純関数正本: gas/yawaragi-board/kasan-core.js
//     （rootDir="." で一緒に push される。KASAN_HEADER 等はそちらの定義を使う＝ここに内包しない）
//   設計書: docs/superpowers/specs/2026-07-17-kasan-app-design.md
// =============================================================
var KASAN_SHEET = 'kasan_master';

// [section, 表示順, 系統, コード, 項目, 値, 最終確認日, 備考]
// ★値は全てテキスト書式のセルへ setValues で入れる（appendRow は使わない）。
var KASAN_MASTER_SEED = [
  ['基本情報', 10, '', '', '法人名', '株式会社キープフィットライフ', '2026-07-17', ''],
  ['基本情報', 20, '', '', '事業所名', 'リハビリデイサービス やわらぎ', '2026-07-17', ''],
  ['基本情報', 30, '', '', 'サービス種類', '地域密着型通所介護', '2026-07-17', ''],
  ['基本情報', 40, '', '', '事業所番号', '1173300995', '2026-07-17', ''],
  ['基本情報', 50, '', '', '所在地', '東松山市高坂', '2026-07-17', ''],
  ['基本情報', 60, '', '', '保険者（介護給付）', '東松山市', '2026-07-17', ''],
  ['基本情報', 70, '', '', '保険者（総合事業）', '東松山市・嵐山町・川島町', '2026-07-17', ''],

  ['運営体制', 10, '', '', '営業時間（1単位）', '9:00–12:00', '2026-07-17', ''],
  ['運営体制', 20, '', '', '営業時間（2単位）', '13:30–16:30', '2026-07-17', ''],
  ['運営体制', 30, '', '', '定員', '各18名', '2026-07-17', ''],

  ['地域区分', 10, '', '', '東松山市の地域区分', '6級地', '2026-07-17', ''],
  ['地域区分', 20, '', '', '1単位単価（介護給付）', '10.27円', '2026-07-17', '6級地の上乗せ6%×通所介護の人件費割合45%＝10円×1.027'],
  ['地域区分', 30, '', '', '地域区分の見直し', '令和6〜8年度は見直しなし', '2026-07-17', ''],
  ['地域区分', 40, '', '', '1単位単価（総合事業）', '未取得', '', '東松山市・嵐山町・川島町の3保険者分。出所＝市の総合事業の手引き／運営推進会議資料'],
  ['地域区分', 50, '', '', '嵐山町・川島町の地域区分', '未取得', '', '総合事業の保険者。東松山市（6級地）のみ確定'],

  ['加算', 10, '介護給付', '781241', '本体（地域通所介護11）', '', '2026-07-17', ''],
  ['加算', 20, '介護給付', '785053', '個別機能訓練加算Ⅰ2', '', '2026-07-17', ''],
  ['加算', 30, '介護給付', '785052', '個別機能訓練加算Ⅱ', '', '2026-07-17', ''],
  ['加算', 40, '介護給付', '786339', 'ADL維持等加算Ⅱ', '', '2026-07-17', ''],
  ['加算', 50, '介護給付', '785608', '口腔機能向上加算Ⅱ', '', '2026-07-17', ''],
  ['加算', 60, '介護給付', '786361', '科学的介護推進体制加算', '', '2026-07-17', ''],
  ['加算', 70, '介護給付', '786099', 'サービス提供体制強化加算Ⅰ', '', '2026-07-17', 'システム表記は「地域通所介護サービス提供体制加算Ⅰ」（略称）'],
  ['加算', 80, '介護給付', '786108', '処遇改善加算Ⅰ', '', '2026-07-17', '令和8年6月〜Ⅰロ 12.7%（令和8年4〜5月分は旧・9.2%）'],

  ['加算', 110, '総合事業', 'A61111', '本体（通所型独自サービス11）', '', '2026-07-17', ''],
  ['加算', 120, '総合事業', 'A65011', '口腔機能向上加算Ⅱ', '', '2026-07-17', ''],
  ['加算', 130, '総合事業', 'A66311', '科学的介護推進体制加算', '', '2026-07-17', ''],
  ['加算', 140, '総合事業', 'A66011', 'サービス提供体制強化加算Ⅰ1', '', '2026-07-17', 'システム表記は「通所型独自サービス提供体制加算Ⅰ１」（略称）'],
  ['加算', 150, '総合事業', 'A66100', '処遇改善加算Ⅰ', '', '2026-07-17', '加算率は未確認（介護給付は12.7%だが総合事業も同率かは未確認）']
];
```

- [ ] **Step 3: setupKasanMaster を追記（社長がエディタで引数なし実行）**

```js
// kasan_master を作り、未投入のシード行だけを足す（冪等＝何度実行しても重複しない）。
// 社長がGASエディタから引数なしで実行する想定（setupDengonBoard と同型）。
function setupKasanMaster(ss) {
  ss = ss || SpreadsheetApp.openById(SS_ID); // エディタ手動実行（引数なし）でも動く
  var sheet = ss.getSheetByName(KASAN_SHEET);
  if (!sheet) {
    sheet = ss.insertSheet(KASAN_SHEET);
    sheet.getRange(1, 1, 1, KASAN_HEADER.length).setValues([KASAN_HEADER]);
    sheet.getRange(1, 1, 1, KASAN_HEADER.length).setFontWeight('bold');
    sheet.setFrozenRows(1);
  }
  // ★全列テキスト書式（設計書 §5.2）。781241 と 1173300995 の数値化、最終確認日の
  //   Date化(+16hずれ)を同時に封じる。setValues の前に必ず適用する。
  sheet.getRange(1, 1, sheet.getMaxRows(), KASAN_HEADER.length).setNumberFormat('@');

  var values = sheet.getDataRange().getValues();
  var seen = {};
  for (var i = 1; i < values.length; i++) seen[kasanSeedKey_(values[i])] = true;

  var toAdd = KASAN_MASTER_SEED.filter(function (row) { return !seen[kasanSeedKey_(row)]; });
  if (toAdd.length) {
    // ★appendRow は使わない（テキスト書式を貫通して型強制が起きる・teishutsu の教訓）
    sheet.getRange(sheet.getLastRow() + 1, 1, toAdd.length, KASAN_HEADER.length).setValues(toAdd);
  }
  return {
    ok: true,
    sheet: KASAN_SHEET,
    added: toAdd.map(function (r) { return kasanSeedKey_(r); }),
    totalRows: Math.max(0, sheet.getLastRow() - 1)
  };
}
```

- [ ] **Step 4: エンドポイント kasan(e) を追記**

```js
// 認証フック。現状は版ゲートのみ運用のため常に true（素通し）＝設計書 §2.1。
// feat/gas-token-auth が master 反映されたら、ここで checkToken() を呼ぶだけで
// kasan も全アプリ横並びで有効化できる（呼び出し側を触らない）。
function kasanAuthOk_(e) { return true; }

// action=kasan : kasan_master を読んでカテゴリ別に返す（読み取り専用・JSONP）。
function kasan(e) {
  var callback = (e && e.parameter) ? e.parameter.callback : null;
  if (!kasanAuthOk_(e)) return respond({ ok: false, error: 'auth_required' }, callback);
  try {
    var sheet = SpreadsheetApp.openById(SS_ID).getSheetByName(KASAN_SHEET);
    if (!sheet) {
      return respond({ ok: false, error: KASAN_SHEET + ' シートがありません。setupKasanMaster を実行してください。' }, callback);
    }
    var rows = kasanSortRows(kasanParseRows(sheet.getDataRange().getValues()));
    var g = kasanGroupBySection(rows);
    return respond({
      ok: true,
      '基本情報': g['基本情報'],
      '運営体制': g['運営体制'],
      '地域区分': g['地域区分'],
      '加算': kasanSplitKeitou(g['加算']),
      '不明': g['不明'],   // 未知sectionも返す＝画面から黙って消さない
      updatedAt: Utilities.formatDate(new Date(), 'Asia/Tokyo', 'yyyy-MM-dd HH:mm')
    }, callback);
  } catch (err) {
    return respond({ ok: false, error: String((err && err.message) || err) }, callback);
  }
}
```

- [ ] **Step 5: doGet に分岐を追記**

`コード.js:1178` の `monthBoard` 分岐の**直後**に追記（既存行は変更しない）:

```js
  // 加算・事業所情報（kasan_master 読み取り専用・整形はkasan-core.jsの純関数）2026-07-17
  if (e && e.parameter && e.parameter.action === 'kasan') {
    return kasan(e);
  }
```

- [ ] **Step 6: additive-only を実測で確認**

```bash
cd /c/tmp/wt-kasan && git diff --numstat -- "gas/yawaragi-board/コード.js"
```

Expected: 削除数が **0**（`<追加数>	0	gas/yawaragi-board/コード.js`）。削除が1行でもあれば既存を壊している＝Step 2〜5 をやり直す。

- [ ] **Step 7: 構文チェック（GASへ送る前に node で落ちないか）**

```bash
cd /c/tmp/wt-kasan && node --check gas/yawaragi-board/コード.js && node --check gas/yawaragi-board/kasan-core.js && echo "構文OK"
```

Expected: `構文OK`

- [ ] **Step 8: 二重宣言がないことを確認（GAS本番停止の防止）**

```bash
cd /c/tmp/wt-kasan && grep -c "^var KASAN_HEADER" gas/yawaragi-board/コード.js gas/yawaragi-board/kasan-core.js
```

Expected: `コード.js:0` と `kasan-core.js:1`（コード.js が再定義していないこと＝GASは全ファイル同一スコープなので二重宣言で停止する）

- [ ] **Step 9: コミット**

```bash
cd /c/tmp/wt-kasan && git add gas/yawaragi-board/コード.js && git commit -m "feat(kasan): GAS結線 action=kasan＋setupKasanMaster（additive-only・全列テキスト書式・setValues）"
```

---

## Task 6: kasan.html（版ゲート＋JSONP＋カード4枚）

**Files:**
- Create: `C:/tmp/wt-kasan/kasan.html`

- [ ] **Step 1: 版ゲートブロックを genba.html から byte 単位で取り出す**

```bash
cd /c/tmp/wt-kasan && node -e "
const fs=require('fs');
const s=fs.readFileSync('genba.html','utf8');
const i=s.indexOf('function gateShouldReload');
const st=s.lastIndexOf('<script>',i), en=s.indexOf('</script>',i);
fs.writeFileSync('/tmp/gate.txt', s.slice(st,en+9));
console.log('抽出 '+ (en+9-st) + ' bytes');
"
```

Expected: `抽出 889 bytes`

- [ ] **Step 2: kasan.html を作成**

`kasan.html`（`<script>function gateShouldReload…</script>` の889バイトは **Step 1 で抽出したものをそのまま貼る**＝1文字も変えない）:

```html
<!DOCTYPE html>
<html lang="ja">
<head>
    <!-- キャッシュ自動更新バージョンゲート（design.md 2026-06-18 準拠・<head>最先頭・genba型/自己完結・shared.js不要） -->
    <!-- version.txt を取得し、現URLの ?v= と違えば1回だけ最新版へ location.replace する -->
    <script>
    function gateShouldReload(cur, latest) { return !!latest && cur !== latest; }
    function buildVersionedUrl(currentUrl, latest) { var u = new URL(currentUrl); u.searchParams.set('v', latest); return u.href; }
    (function () {
        try {
            var cur = new URLSearchParams(location.search).get('v');
            fetch('version.txt?_=' + Date.now(), { cache: 'no-store' })
                .then(function (r) { return r.text(); })
                .then(function (t) {
                    var latest = (t || '').trim();
                    if (gateShouldReload(cur, latest)) {
                        location.replace(buildVersionedUrl(location.href, latest));
                    }
                })
                .catch(function () { /* オフライン等は no-op で固まらせない */ });
        } catch (e) { /* 念のため no-op（ゲートで画面を固まらせない） */ }
    })();
    </script>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="Cache-Control" content="no-cache, no-store, must-revalidate">
    <meta http-equiv="Pragma" content="no-cache">
    <meta http-equiv="Expires" content="0">
    <title>加算・事業所情報｜やわらぎ</title>
<style>
  :root{
    --ground:#eef2f6; --surface:#ffffff; --surface-2:#f5f8fb;
    --ink:#243239; --ink-2:#516470; --muted:#93a3ad;
    --accent:#2c7a7b;
    --line:#e3e9ee;
    --warn:#e08a1e; --warn-bg:#fdf1df;
    --crit:#e04338; --crit-bg:#fcebe9;
    --mono: ui-monospace, SFMono-Regular, Menlo, Consolas, "Courier New", monospace;
  }
  *{box-sizing:border-box;}
  body{margin:0;background:var(--ground);color:var(--ink);
       font-family:-apple-system,BlinkMacSystemFont,"Hiragino Sans","Noto Sans JP",sans-serif;
       font-size:15px;line-height:1.7;-webkit-text-size-adjust:100%;}
  header{background:var(--surface);border-bottom:1px solid var(--line);padding:13px 16px;}
  h1{margin:0;font-size:17px;font-weight:700;letter-spacing:.02em;}
  .sub{margin:2px 0 0;font-size:12px;color:var(--muted);}
  main{max-width:820px;margin:0 auto;padding:14px 16px;}
  .card{background:var(--surface);border:1px solid var(--line);border-radius:10px;
        margin-bottom:14px;overflow:hidden;}
  .card > h2{margin:0;padding:11px 14px;font-size:14px;font-weight:700;
             background:var(--surface-2);border-bottom:1px solid var(--line);color:var(--ink-2);}
  .card > .body{padding:4px 14px 10px;}
  .kv{display:flex;gap:12px;padding:8px 0;border-bottom:1px solid var(--line);}
  .kv:last-child{border-bottom:none;}
  .k{flex:0 0 40%;color:var(--ink-2);font-size:13px;}
  .v{flex:1;font-weight:600;word-break:break-word;}
  .unknown{color:#8a5a12;background:var(--warn-bg);border:1px solid var(--warn);
           border-radius:4px;padding:0 6px;font-weight:700;font-size:13px;}
  .keitou{margin:12px 0 0;padding:6px 0 6px 9px;border-left:3px solid var(--accent);
          font-size:13px;font-weight:700;color:var(--accent);}
  .keitou.bad{border-left-color:var(--warn);color:#8a5a12;}
  .krow{display:flex;gap:11px;align-items:baseline;padding:8px 0;border-bottom:1px solid var(--line);}
  .krow:last-child{border-bottom:none;}
  .code{font-family:var(--mono);font-size:14px;font-weight:700;
        font-variant-numeric:tabular-nums;letter-spacing:.01em;
        flex:0 0 auto;min-width:5.8em;}
  .kname{flex:1;font-weight:600;}
  .note{display:block;font-size:12px;color:var(--muted);font-weight:400;margin-top:2px;line-height:1.5;}
  #loadInd{padding:12px 16px;color:var(--muted);font-size:13px;}
  #err{margin:12px 16px;padding:10px 12px;border-radius:8px;background:var(--crit-bg);
       border:1px solid var(--crit);color:#a3271e;font-size:13px;display:none;}
  .foot{color:var(--muted);font-size:11px;text-align:center;padding:2px 0 24px;}
</style>
</head>
<body>
<header>
  <h1>加算・事業所情報</h1>
  <p class="sub">リハビリデイサービス やわらぎ ／ ケアマネ対応用にサービスコードを併記</p>
</header>
<div id="loadInd">⟳ 読み込み中…</div>
<div id="err"></div>
<main id="app"></main>

<script>
  // ===== 設定 =====
  var GAS = 'https://script.google.com/macros/s/AKfycbwo1UGxsK1qgmO8IDaqT-inDM0Qgoe_MRvxfKDxHy_gXANi4FwNFlgn2pEanMXVQxsdlw/exec';

  function esc(s){ return String(s==null?'':s).replace(/[&<>"]/g, function(c){ return {'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]; }); }
  function setLoad(on){ var el=document.getElementById('loadInd'); if(el) el.textContent = on ? '⟳ 読み込み中…' : ''; }
  function showError(msg){ var el=document.getElementById('err'); if(!el) return; el.textContent='⚠ '+msg; el.style.display='block'; }

  // 「未取得」「未確認」は警告色で出す＝確定値と見分けがつく状態にする（設計書 §2.2/§5.3）
  function valHtml(v){
    var s = String(v==null?'':v);
    if (s === '未取得' || s === '未確認') return '<span class="unknown">'+esc(s)+'</span>';
    return esc(s);
  }

  function noteHtml(n){ return n ? '<span class="note">'+esc(n)+'</span>' : ''; }

  function kvCard(title, rows){
    if (!rows || !rows.length) return '';
    var h = '<section class="card"><h2>'+esc(title)+'</h2><div class="body">';
    rows.forEach(function(r){
      h += '<div class="kv"><div class="k">'+esc(r.item)+'</div>'
        +  '<div class="v">'+valHtml(r.value)+noteHtml(r.note)+'</div></div>';
    });
    return h + '</div></section>';
  }

  function krows(rows){
    return (rows||[]).map(function(r){
      return '<div class="krow"><span class="code">'+esc(r.code)+'</span>'
           + '<span class="kname">'+esc(r.item)+noteHtml(r.note)+'</span></div>';
    }).join('');
  }

  function kasanCard(split){
    var h = '<section class="card"><h2>加算</h2><div class="body">';
    h += '<div class="keitou">介護給付（地域密着型通所介護）</div>' + krows(split['介護給付']);
    h += '<div class="keitou">総合事業（通所型独自サービス）</div>' + krows(split['総合事業']);
    var u = split['系統不明'] || [];
    if (u.length){
      h += '<div class="keitou bad">⚠ 系統不明（kasan_master の系統列を確認）</div>' + krows(u);
    }
    return h + '</div></section>';
  }

  function render(d){
    var h = kvCard('事業所基本情報', d['基本情報'])
          + kvCard('運営体制', d['運営体制'])
          + kvCard('地域区分', d['地域区分'])
          + kasanCard(d['加算'] || {});
    if (d['不明'] && d['不明'].length){
      h += kvCard('⚠ 未知のsection（kasan_master を確認）', d['不明']);
    }
    h += '<p class="foot">正本＝利用者台帳SS「kasan_master」／ 取得 '+esc(d.updatedAt||'')+'</p>';
    document.getElementById('app').innerHTML = h;
  }

  // JSONP（board GAS は GET=JSONP・POST=no-cors。genba/session-board と同型）
  function fetchKasan(){
    setLoad(true);
    var cbName = 'kasanCb_' + Date.now();
    var done = false, s = null;
    var timer = setTimeout(function(){ if(done) return; done=true; cleanup(); setLoad(false); showError('タイムアウト'); }, 20000);
    function cleanup(){
      try { delete window[cbName]; } catch(e){ window[cbName] = undefined; }
      if (s && s.parentNode) s.parentNode.removeChild(s);
      clearTimeout(timer);
    }
    window[cbName] = function(resp){
      if (done) return; done = true; cleanup(); setLoad(false);
      if (!resp || resp.ok === false){ showError((resp && resp.error) || '不明なエラー'); return; }
      render(resp);
    };
    s = document.createElement('script');
    s.src = GAS + '?action=kasan&callback=' + cbName + '&t=' + Date.now();
    s.onerror = function(){ if(done) return; done=true; cleanup(); setLoad(false); showError('通信エラー'); };
    document.head.appendChild(s);
  }

  fetchKasan();
</script>
</body>
</html>
```

- [ ] **Step 3: 構文チェック**

```bash
cd /c/tmp/wt-kasan && node -e "
const fs=require('fs');
const h=fs.readFileSync('kasan.html','utf8');
const blocks=[...h.matchAll(/<script>([\s\S]*?)<\/script>/g)].map(m=>m[1]);
blocks.forEach((b,i)=>{ new Function(b); console.log('script['+i+'] 構文OK ('+b.length+' chars)'); });
"
```

Expected: `script[0] 構文OK` と `script[1] 構文OK` の2行

- [ ] **Step 4: コミット**

```bash
cd /c/tmp/wt-kasan && git add kasan.html && git commit -m "feat(kasan): kasan.html（版ゲートgenba移植・JSONP取得・カード4枚・コード等幅併記）"
```

---

## Task 7: HTML テスト（版ゲート byte 一致＋静的検証）

**Files:**
- Create: `C:/tmp/wt-kasan/scripts/test-kasan-version-gate.js`
- Create: `C:/tmp/wt-kasan/scripts/test-kasan-html.js`

- [ ] **Step 1: 版ゲート byte 一致テストを書く**

`scripts/test-kasan-version-gate.js`:

```js
// kasan.html のキャッシュ自動更新バージョンゲート横展開テスト
// 方式: genba.html を正本とし、kasan.html の <head> 最先頭ゲートが
//   (1) genba のゲート <script> ブロックと byte 単位で同一（verbatim 移植）
//   (2) 抽出した純関数 gateShouldReload / buildVersionedUrl が期待通り動く
// を実コード抽出で検証する（出荷コードとテストのドリフト防止）。
// 実行: node scripts/test-kasan-version-gate.js
const fs = require('fs');
const path = require('path');
let pass = 0, fail = 0;
function ok(cnd, m){ if(cnd){pass++;console.log('  PASS '+m);}else{fail++;console.log('  FAIL '+m);} }

function gateBlock(file){
  const s = fs.readFileSync(path.join(__dirname, '..', file), 'utf8');
  const i = s.indexOf('function gateShouldReload');
  if (i < 0) return null;
  const st = s.lastIndexOf('<script>', i);
  const en = s.indexOf('</script>', i);
  return s.slice(st, en + 9);
}

console.log('[版ゲート byte一致]');
const G = gateBlock('genba.html');
const K = gateBlock('kasan.html');
ok(G !== null, 'genba.html からゲート抽出できる');
ok(K !== null, 'kasan.html からゲート抽出できる');
ok(G === K, '★kasan.html のゲートが genba.html と byte 単位で同一（verbatim移植）');

console.log('\n[ゲートが <head> 最先頭にある]');
const KH = fs.readFileSync(path.join(__dirname, '..', 'kasan.html'), 'utf8');
const headAt = KH.indexOf('<head>');
const gateAt = KH.indexOf('function gateShouldReload');
const charsetAt = KH.indexOf('<meta charset');
ok(headAt >= 0 && gateAt > headAt, 'ゲートは <head> の内側');
ok(gateAt < charsetAt, '★ゲートは <meta charset> より前＝最先頭（他資産の読込前に効く）');

console.log('\n[抽出した純関数の挙動]');
const fn = new Function(K.replace(/^<script>/, '').replace(/<\/script>$/, '') +
  '; return { gateShouldReload: gateShouldReload, buildVersionedUrl: buildVersionedUrl };');
// 即時実行の (function(){...})() は location/fetch が無い node でも try/catch で握られる
const g = fn();
ok(g.gateShouldReload(null, '2026-07-04-71') === true, '?v=無し→リロードする');
ok(g.gateShouldReload('2026-07-04-70', '2026-07-04-71') === true, '版が古い→リロードする');
ok(g.gateShouldReload('2026-07-04-71', '2026-07-04-71') === false, '版が同じ→リロードしない');
ok(g.gateShouldReload('2026-07-04-70', '') === false, '★latestが空→リロードしない（version.txt取得失敗でループしない）');
ok(g.buildVersionedUrl('https://x.io/kasan.html', '2026-07-04-71') === 'https://x.io/kasan.html?v=2026-07-04-71', '?v= を付ける');
ok(g.buildVersionedUrl('https://x.io/kasan.html?v=old', '2026-07-04-71') === 'https://x.io/kasan.html?v=2026-07-04-71', '既存の ?v= を置換（増殖しない）');

console.log('\n===== ' + pass + ' passed / ' + fail + ' failed =====');
process.exit(fail ? 1 : 0);
```

- [ ] **Step 2: 走らせて通ることを確認**

Run: `cd /c/tmp/wt-kasan && node scripts/test-kasan-version-gate.js`
Expected: `===== 11 passed / 0 failed =====`

- [ ] **Step 3: 静的検証テストを書く**

`scripts/test-kasan-html.js`:

```js
// kasan.html 静的検証（jsdom は未導入なので使わない・実ファイルの中身を検査する）
// 実行: node scripts/test-kasan-html.js
const fs = require('fs');
const path = require('path');
const H = fs.readFileSync(path.join(__dirname, '..', 'kasan.html'), 'utf8');
let pass = 0, fail = 0;
function ok(cnd, m){ if(cnd){pass++;console.log('  PASS '+m);}else{fail++;console.log('  FAIL '+m);} }

console.log('[★通信方式 JSONP（board GAS は GET=JSONP・fetchでは動かない）]');
ok(/createElement\('script'\)/.test(H), 'script要素注入で取得している');
ok(/action=kasan&callback=/.test(H), '★callback= を付けている（無いとJSONPにならない）');
ok(!/fetch\(\s*GAS/.test(H), '★GASへ fetch していない（CORSで失敗する経路を作らない）');
ok(/s\.onerror/.test(H), '通信エラーを拾っている');
ok(/setTimeout\(/.test(H) && /タイムアウト/.test(H), 'タイムアウトで固まらせない');
ok(/delete window\[cbName\]/.test(H), 'コールバックを掃除している（グローバル汚染しない）');

console.log('\n[版ゲートは fetch で正しい（version.txt は同一オリジン）]');
ok(/fetch\('version\.txt/.test(H), "version.txt は fetch で取る（同一オリジンなのでOK）");
ok(/cache: 'no-store'/.test(H), 'version.txt は no-store で取る');

console.log('\n[XSS: 出力は全て esc を通す]');
ok(/function esc\(/.test(H), 'esc() が定義されている');
// innerHTML に入る式が esc/valHtml/noteHtml/krows/kvCard/kasanCard 以外の生値を含まないこと
ok(/document\.getElementById\('app'\)\.innerHTML = h;/.test(H), 'innerHTML への代入は組み立て済みの h のみ');
ok(!/innerHTML\s*=\s*[^h;]*\br\.(item|value|code|note)\b/.test(H), '★r.item/r.value/r.code/r.note を生で innerHTML に入れていない');
ok(/esc\(r\.item\)/.test(H), 'r.item は esc 経由');
ok(/esc\(r\.code\)/.test(H), 'r.code は esc 経由');
ok(/esc\(s\)/.test(H), 'valHtml 内で esc 経由');
ok(/esc\(n\)/.test(H), 'noteHtml 内で esc 経由');
ok(/esc\(d\.updatedAt/.test(H), 'updatedAt も esc 経由');

console.log('\n[未取得/未確認は確定値と見分けがつく]');
ok(/'未取得'/.test(H) && /'未確認'/.test(H), '未取得・未確認を判定している');
ok(/class="unknown"/.test(H), '警告スタイルを当てている');

console.log('\n[2系統を明確に区切る（指示書の要件）]');
ok(/介護給付（地域密着型通所介護）/.test(H), '介護給付の見出し');
ok(/総合事業（通所型独自サービス）/.test(H), '総合事業の見出し');
ok(/border-left:3px solid var\(--accent\)/.test(H), '系統見出しに罫線');
ok(/font-family:var\(--mono\)/.test(H), '★コードは等幅');
ok(/font-variant-numeric:tabular-nums/.test(H), '★数字の桁が揃う');

console.log('\n[未知データを黙って消さない]');
ok(/系統不明/.test(H), '系統不明を描画する');
ok(/未知のsection/.test(H), '未知sectionを描画する');

console.log('\n[トーン統一（紫グラデ・過剰絵文字を使わない）]');
ok(!/linear-gradient/.test(H), '★グラデーションを使っていない');
ok(!/#6b46c1|#805ad5|#9f7aea|purple/i.test(H), '★紫系の色を使っていない');
ok(/--accent:#2c7a7b/.test(H), 'session-board と同じアクセント色');

console.log('\n===== ' + pass + ' passed / ' + fail + ' failed =====');
process.exit(fail ? 1 : 0);
```

- [ ] **Step 4: 走らせて通ることを確認**

Run: `cd /c/tmp/wt-kasan && node scripts/test-kasan-html.js`
Expected: `===== 28 passed / 0 failed =====`（**合格条件は `0 failed`**。件数はアサート追加で増えてよい）

- [ ] **Step 5: 全テストをまとめて走らせる**

```bash
cd /c/tmp/wt-kasan && node scripts/test-kasan-core.js && node scripts/test-kasan-version-gate.js && node scripts/test-kasan-html.js && echo "=== kasan 全テスト緑 ==="
```

Expected: 3本とも `0 failed` ＋ `=== kasan 全テスト緑 ===`

- [ ] **Step 6: 既存テストの回帰確認（コード.js を触ったため）**

```bash
cd /c/tmp/wt-kasan && for t in scripts/test-session-board-core.js scripts/test-month-board-core.js scripts/test-attendance-view-core.js; do echo "--- $t"; node "$t" 2>&1 | tail -1; done
```

Expected: 3本とも `0 failed`

- [ ] **Step 7: コミット**

```bash
cd /c/tmp/wt-kasan && git add scripts/test-kasan-version-gate.js scripts/test-kasan-html.js && git commit -m "test(kasan): 版ゲートbyte一致＋HTML静的検証（JSONP方式・esc網羅・トーン統一）"
```

---

## Task 8: 社長向けリリース手順書を作る

**Files:**
- Create: `C:/tmp/wt-kasan/docs/superpowers/plans/2026-07-17-kasan-release-runbook.md`

- [ ] **Step 1: 手順書を書く**

`docs/superpowers/plans/2026-07-17-kasan-release-runbook.md`:

```markdown
# kasan.html リリース手順（社長の手で実行）

## 0. 前提
- ブランチ: `feat/kasan-app`（worktree `C:/tmp/wt-kasan`）
- 全テスト緑を確認済み（core / version-gate / html）
- **clasp 操作は Bash サンドボックス無効で実行**（memory: `dangerouslyDisableSandbox` 必須）

## 1. GASへ push（★本番pull突合を先に）
```
cd /c/tmp/wt-kasan/gas/yawaragi-board
clasp pull
git diff --stat        # ← 平文LINEトークン / dengon seed のドリフトが出る。消さない
clasp push
```

## 2. シードを投入（GASエディタで実行）
1. GASエディタを開く（板GAS所有＝m-higa@keepfitlife.com。URLに `?authuser=` を付ける）
2. 関数 `setupKasanMaster` を選び「実行」（引数なしで動く）
3. 戻り値 `added` が **28件**、`totalRows` が **28** であることを確認
4. スプレッドシート「kasan_master」タブを目視:
   - コード列が `781241`（左寄せ＝テキスト）になっている。右寄せ（＝数値）ならテキスト書式が効いていない
   - 事業所番号が `1173300995` のまま（指数表記になっていない）
5. **再実行して冪等を確認**: `added` が **0件**、`totalRows` は **28のまま**

## 3. デプロイ（URL維持）
```
clasp deploy -i AKfycbwo1UGxsK1qgmO8IDaqT-inDM0Qgoe_MRvxfKDxHy_gXANi4FwNFlgn2pEanMXVQxsdlw
```
★新規デプロイを作らない（URLが変わって全アプリが壊れる）

## 4. エンドポイント実測
ブラウザで開く（`callback` 必須・無いと404）:
```
https://script.google.com/macros/s/AKfycbwo…/exec?action=kasan&callback=cb
```
確認: `cb({"ok":true,…})` が返り、加算が **介護給付8件・総合事業5件**、`系統不明` が **0件**

## 5. master反映と版上げ
```
cd /c/tmp/wt-kasan
git fetch origin
git rebase origin/master          # non-FFは正常。--force は絶対に使わない
node scripts/bump-app-version.js 2026-07-04-71
```
★版番号は **日付プレフィックス `2026-07-04` 固定**。今日の日付にしない。
　bumpスクリプトの検証は `/^[0-9A-Za-z._-]+$/` だけで**誤った版番号を素通しさせる**。
　push直前に `git fetch` し直して `-71` が空いているか再確認（`-62` 衝突の再発防止）。

```
git push origin feat/kasan-app:master
node scripts/bump-app-version.js --verify 2026-07-04-71
```

## 6. アプリ台帳へ登録（コード変更不要）
社長専用SS（ScriptProperties `APPREGISTRY_SS_ID`）の「アプリ台帳」シートに1行append:

| 列 | 値 |
|---|---|
| アプリ名 | 加算・事業所情報 |
| カテゴリ | （既存の並びに合わせる） |
| 説明 | やわらぎの加算13件と事業所基本情報。ケアマネ対応用にサービスコード併記 |
| スタッフ用URL | https://m-higa-sys.github.io/yawaragi-apps/kasan.html |
| 公開区分 | staff |
| icon | 📋 |
| 表示順 | （カテゴリ内の末尾） |

→ portal.html は `getAppRegistry?scope=staff` から自動生成されるので、**1行足せば自動で載る**。

## 7. 完了確認（証跡3点）
- [ ] `bump-app-version.js --verify 2026-07-04-71` が成功
- [ ] `?action=kasan&callback=cb` が加算13件を返す
- [ ] portal に「加算・事業所情報」タイルが出て、開くと4カード表示される

## ロールバック
- GAS: `clasp deploy -V <前のバージョン番号>`
- HTML: `git revert <SHA>` → 版を1つ上げて再push
- シート: `kasan_master` タブを削除（他機能から参照されていないので影響なし）
```

- [ ] **Step 2: コミット**

```bash
cd /c/tmp/wt-kasan && git add docs/superpowers/plans/2026-07-17-kasan-release-runbook.md && git commit -m "docs(kasan): 社長向けリリース手順書（pull突合→seed→deploy -i→版71→台帳append）"
```

- [ ] **Step 3: ブランチの全体像を確認して社長へ引き渡す**

```bash
cd /c/tmp/wt-kasan && git log --oneline origin/master..HEAD && git diff --numstat origin/master..HEAD
```

Expected: 8コミット前後。`コード.js` の削除数が **0**（additive-only の最終確認）

**★ここで止まる。** `clasp push` / `clasp deploy` / `git push origin master` は社長が実行する。

---

## Self-Review（この計画を書いた後の確認結果）

**1. Spec coverage** — 設計書の各節に対応するタスク:

| 設計書 | タスク |
|---|---|
| §2.1 認証＝版ゲートのみ | Task 5 Step 4（`kasanAuthOk_` フック）・Task 6（版ゲート移植） |
| §2.2 単価10.27円（介護給付のみ） | Task 5 Step 2（地域区分 表示順20） |
| §2.3 総合事業3保険者の級地・単価が空白 | Task 5 Step 2（表示順40/50＝「未取得」）・Task 6（`valHtml` の警告表示） |
| §3 コード13件 | Task 5 Step 2（加算 表示順10〜150） |
| §4.0 JSONP | Task 5 Step 4（`respond`）・Task 6（script注入）・Task 7（fetch禁止の検証） |
| §4.0.1 版ゲート正本=genba | Task 6 Step 1・Task 7 Step 1 |
| §5 スキーマ | Task 2（`kasanParseRows`）・Task 5 Step 2 |
| §5.2 全列テキスト＋setValues | Task 5 Step 3・Task 1（`kasanNormalizeCode` の二重防御） |
| §5.3 備考（処遇改善率・略称注記） | Task 5 Step 2（786108/A66100/786099/A66011） |
| §6 純関数5つ | Task 1〜4 |
| §7 UI カード4枚 | Task 6 |
| §8 リリース手順 | Task 8 |
| §10 未解決4件 | Task 5 Step 2（シートに「未取得」「未確認」として載る） |

**2. Placeholder scan** — TBD/TODO/「適切に処理」の類なし。全ステップに実コードまたは実コマンドと期待出力あり。

**3. Type consistency** — 関数名・プロパティ名の一貫性を確認:
- core が返す行オブジェクトのキー `{section, order, keitou, code, item, value, checkedAt, note}` は Task 2 で定義し、Task 3（`a.order`/`a.code`）・Task 4（`r.section`/`r.keitou`）・Task 6（`r.item`/`r.value`/`r.code`/`r.note`）で同一。
- `kasanSeedKey_` は Task 1 で定義し Task 5 の `setupKasanMaster` が使用。
- `KASAN_HEADER` は core のみで定義（Task 1）、コード.js は参照のみ（Task 5 Step 8 で二重宣言0を実測）。
- GAS が返すキー `{ok, 基本情報, 運営体制, 地域区分, 加算, 不明, updatedAt}`（Task 5 Step 4）は Task 6 の `render` が読むキーと一致。
- `加算` の中身 `{介護給付, 総合事業, 系統不明}`（Task 4 の `kasanSplitKeitou`）は Task 6 の `kasanCard` が読むキーと一致。

**4. テスト件数の期待値** — Task 1:14 → Task 2:25 → Task 3:29 → **Task 4:42**（累積）。Task 7 で version-gate:11・html:28。**合計 81件**。

**5. 計画のコードを机上で終わらせず実測した（2026-07-17）**

この計画に書いた純関数とテストは**実際に走らせて確認済み**。「動くはず」ではない。

- `kasan-core.js` の Task 1〜4 の全コードをスクラッチパッドに書き出し、Task 1〜4 の全テストを実行 → **42 passed / 0 failed**。累計の期待値（14→25→29→42）も実測と一致。
  - ★この実測で**当初 Task 4 を「41」と書いていた数え間違いを検出し 42 へ修正**した。
- Task 7 の版ゲート eval 検証（`new Function` でゲートブロックを評価）を `genba.html` の実ブロックで実行 → 6アサート全て期待通り。
  `location` / `fetch` が無い node でもゲートの IIFE は try/catch に握られて落ちないことを実測で確認済み。
- `genba.html` と `session-board.html` のゲートが **byte 完全一致（889バイト）** であることも実測済み。

**未実測**: `kasan.html` 本体（Task 6）と `test-kasan-html.js`（Task 7）は、ファイルが存在しないため静的検証の28アサートを走らせていない。GAS 側（Task 5）も本番でしか動かない。実装時に Step ごとの Expected で確認すること。
