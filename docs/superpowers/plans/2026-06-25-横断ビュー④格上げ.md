# 横断ビュー（④ケアマネ送付チェックリスト 格上げ）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 実績作業で「今月どの事業所に・誰の・何の書類を出すか／出したか」を1画面で見る読み取り専用の横断ビューを、既存④に追加する。

**Architecture:** ④フロント（HTML/JS）のみ改修。本番GASは無変更で既存 get 系を読むだけ。当月判定の正準関数を `shared.js` に集約し④だけが読む（既存アプリは `shared.js?v=` 版固定で無影響）。通所モニタリング（getMonitoringYear）を取込み、事業所別⇄利用者別の切替トグルを追加する。

**Tech Stack:** 素のHTML/JS（フレームワーク無し）、Google Apps Script（読み取りのみ）、node（純関数のTDD）。

**設計書:** `docs/superpowers/specs/2026-06-25-横断ビュー④格上げ-design.md`

---

## File Structure

- `shared.js` — 当月判定の正準関数を追加（§I 新設）。**追加のみ・既存は無変更**。
- `scripts/test-cycle-judge.js` — 新規。shared.js から判定関数を実コード抽出してnode検証（drift防止＝既存 test-version-gate.js と同流儀）。
- `ケアマネ送付チェックリスト.html` — shared.js読込追加／fetchAllに getMonitoringYear追加／buildTargetsで個訓判定を正準化＋通所モニ取込／renderにビュー切替トグル＋近似注記。

---

## Task 1: shared.js に当月判定の正準関数を追加（TDD）

**Files:**
- Test: `scripts/test-cycle-judge.js`（Create）
- Modify: `shared.js`（末尾に §I を追加）

正準シグネチャ（①②の実装を移植）:
- `isPlanMonth(planStart, planMonths, year, month)` — ①個訓 計画月（planStart起点、既定3ヶ月／変則は開始月のみ）
- `isHyoukaMonth(planStart, planMonths, year, month)` — ①個訓 評価月（翌々月＋開始前月）
- `isOralEvalMonth(startedAt, year, month)` — ②口腔 評価月（startedAt起点3ヶ月毎）

- [ ] **Step 1: 失敗するテストを書く**

Create `scripts/test-cycle-judge.js`:

```javascript
// test-cycle-judge.js
// shared.js の当月判定 正準関数を「実コード抽出」して node で検証する。
// 判定関数はネスト波括弧を持つため、ブレース対応の抽出器を使う
// （test-version-gate.js の単一return前提の正規表現は使えない）。
// 実行: node scripts/test-cycle-judge.js

const fs = require('fs');
const path = require('path');

const SHARED_PATH = path.join(__dirname, '..', 'shared.js');
const src = fs.readFileSync(SHARED_PATH, 'utf8');

// function NAME(...) { ... } を波括弧の対応を数えて切り出す
function extractFn(name) {
  const sig = 'function ' + name;
  const start = src.indexOf(sig);
  if (start < 0) throw new Error('shared.js に ' + sig + ' が無い（未実装＝RED）');
  const braceOpen = src.indexOf('{', start);
  let depth = 0, i = braceOpen;
  for (; i < src.length; i++) {
    if (src[i] === '{') depth++;
    else if (src[i] === '}') { depth--; if (depth === 0) { i++; break; } }
  }
  return src.slice(start, i);
}

const sandbox = {};
const code = extractFn('isPlanMonth') + '\n' + extractFn('isHyoukaMonth') + '\n' +
  extractFn('isOralEvalMonth') + '\n' +
  'sandbox.isPlanMonth = isPlanMonth; sandbox.isHyoukaMonth = isHyoukaMonth;' +
  'sandbox.isOralEvalMonth = isOralEvalMonth;';
(function () { eval(code); })();
const { isPlanMonth, isHyoukaMonth, isOralEvalMonth } = sandbox;

let pass = 0, fail = 0;
function eq(actual, expected, label) {
  if (actual === expected) { pass++; console.log('  PASS ' + label); }
  else { fail++; console.log('  FAIL ' + label + ' :: exp=' + JSON.stringify(expected) + ' act=' + JSON.stringify(actual)); }
}

console.log('[isPlanMonth] 既定3ヶ月: planStart=2026-04, planMonths=3');
eq(isPlanMonth('2026-04', 3, 2026, 4), true,  '開始月=計画月');
eq(isPlanMonth('2026-04', 3, 2026, 7), true,  '+3ヶ月=計画月');
eq(isPlanMonth('2026-04', 3, 2026, 6), false, '+2ヶ月=非計画月');
eq(isPlanMonth('2026-04', 3, 2026, 3), false, '開始前月=非計画月');
eq(isPlanMonth('', 3, 2026, 4), false, 'planStart空=false');
console.log('[isPlanMonth] 変則: planMonths=2 は開始月のみ');
eq(isPlanMonth('2026-04', 2, 2026, 4), true,  '変則: 開始月のみtrue');
eq(isPlanMonth('2026-04', 2, 2026, 6), false, '変則: 自動リピートなし');

console.log('[isHyoukaMonth] 既定3ヶ月: planStart=2026-04');
eq(isHyoukaMonth('2026-04', 3, 2026, 6), true,  '翌々月=評価月');
eq(isHyoukaMonth('2026-04', 3, 2026, 9), true,  '+3で評価月');
eq(isHyoukaMonth('2026-04', 3, 2026, 3), true,  '開始前月も評価月（前計画評価）');
eq(isHyoukaMonth('2026-04', 3, 2026, 4), false, '開始月=非評価月');
eq(isHyoukaMonth('2026-04', 3, 2026, 7), false, '+3ヶ月（計画月）=非評価月');
console.log('[isHyoukaMonth] 変則: planMonths=2 → 計画最終月が評価月');
eq(isHyoukaMonth('2026-04', 2, 2026, 5), true,  '変則: 開始+1（最終月）=評価月');

console.log('[isOralEvalMonth] startedAt=2026-06 起点3ヶ月毎');
eq(isOralEvalMonth('2026-06', 2026, 6), true,  '開始月=評価月');
eq(isOralEvalMonth('2026-06', 2026, 9), true,  '+3=評価月');
eq(isOralEvalMonth('2026-06', 2026, 7), false, '+1=非評価月');
eq(isOralEvalMonth('2026-06', 2026, 5), false, '開始前=false');
eq(isOralEvalMonth('', 2026, 6), false, 'startedAt空=false');

console.log('\n' + pass + ' PASS / ' + fail + ' FAIL');
if (fail > 0) process.exit(1);
```

- [ ] **Step 2: テストを実行して失敗を確認**

Run: `node scripts/test-cycle-judge.js`
Expected: FAIL（`shared.js に function isPlanMonth が無い（未実装＝RED）` で throw）

- [ ] **Step 3: shared.js に §I を追加（①②から移植）**

`shared.js` の末尾（397行目 `_isAbsenceAction` 関数の後）に追記:

```javascript

/* ===== §I cycle-judge（加算サイクル当月判定・2026-06-25 横断ビュー④用に集約） =====
   個訓(①)・口腔(②)の当月判定の唯一の置き場。④がこれを読む。
   ①②本体はまだ自前定義を持つ（次フェーズで本関数へ切替）。本関数は純関数・DOM非依存。 */

// 個訓 計画月: planStart起点。既定3ヶ月は diff%3===0、変則(planMonths 1-12)は開始月のみ。
// ①個別機能訓練計画書チェック.html の同名関数を移植（無回帰のため完全同一ロジック）。
function isPlanMonth(planStart, planMonths, year, month) {
    if (!planStart) return false;
    const m = String(planStart).match(/^(\d{4})-(\d{2})$/);
    if (!m) return false;
    const py = parseInt(m[1], 10);
    const pm = parseInt(m[2], 10);
    const diff = (year - py) * 12 + (month - pm);
    const pmNum = parseInt(planMonths, 10);
    const L = (pmNum >= 1 && pmNum <= 12) ? pmNum : 3;
    if (L === 3) return diff >= 0 && diff % 3 === 0;
    return diff === 0;
}

// 個訓 評価月: 計画スタート月の翌々月（=次計画前月）。開始前月(diff===-1)も評価月扱い。
// ①の同名関数を移植。
function isHyoukaMonth(planStart, planMonths, year, month) {
    if (!planStart) return false;
    const m = String(planStart).match(/^(\d{4})-(\d{2})$/);
    if (!m) return false;
    const py = parseInt(m[1], 10);
    const pm = parseInt(m[2], 10);
    const diff = (year - py) * 12 + (month - pm);
    const pmNum = parseInt(planMonths, 10);
    const L = (pmNum >= 1 && pmNum <= 12) ? pmNum : 3;
    if (L === 3) {
        if (diff >= 2 && diff % 3 === 2) return true;
        if (diff === -1) return true;
        return false;
    }
    if (diff === (L - 1)) return true;
    if (diff === -1) return true;
    return false;
}

// 口腔 評価月: startedAt起点3ヶ月毎。②oral.html の同名関数を移植。
function isOralEvalMonth(startedAt, year, month) {
    const m = String(startedAt || '').match(/^(\d{4})-(\d{2})/);
    if (!m) return false;
    const sTotal = parseInt(m[1], 10) * 12 + parseInt(m[2], 10);
    const tTotal = year * 12 + month;
    if (tTotal < sTotal) return false;
    return (tTotal - sTotal) % 3 === 0;
}
```

- [ ] **Step 4: テストを実行して成功を確認**

Run: `node scripts/test-cycle-judge.js`
Expected: PASS（`20 PASS / 0 FAIL`）

- [ ] **Step 5: Commit**

```bash
git add shared.js scripts/test-cycle-judge.js
git commit -m "feat(shared): 加算サイクル当月判定(isPlanMonth/isHyoukaMonth/isOralEvalMonth)を①②から集約+TDD"
```

---

## Task 2: ④が shared.js を読み、個訓評価判定を①と同源に正準化

**Files:**
- Modify: `ケアマネ送付チェックリスト.html`（head・fetchAll・buildTargets・自前 isHyoukaMonth 削除）

**狙い:** ④の個訓評価判定を、①と同じデータ源（getKeikakushoYear の users＝planStart＋planMonths）＋同じ正準関数に揃え、完了条件2（①と件数一致）を満たす。④自前の `isHyoukaMonth(planStart, ym)`(626行) は planMonths非対応の drift版なので削除する（残すと後勝ちで shared.js 版を shadow する）。

- [ ] **Step 1: 名前衝突が無いことを確認（安全確認）**

Run:
```bash
grep -nE "^\s*(const|let)\s+(openModal|closeModal|showToast|gasPost|DAY_NAMES|YAWARAGIBOARD_API_URL|ATT_CACHE_KEY|UserStore|AttStore)\b" "ケアマネ送付チェックリスト.html"
```
Expected: 出力なし（空）。`const showToast` 等が無ければ shared.js の `function` 定義と衝突しない。
※ もし出力があれば、その名前は④側で `const`/`let` 定義されており shared.js の `function` 宣言とSyntaxError衝突する。その場合は実装を止めて報告（このプランの前提が崩れる）。

- [ ] **Step 2: head に shared.js 読込を追加**

`ケアマネ送付チェックリスト.html` の `<script>`（427行目）の**直前**に追加:

```html
<!-- 当月判定の正準関数（§I cycle-judge）。本体scriptより前に読む契約。?v=で④だけ新版を取得 -->
<script src="shared.js?v=2026-06-25a"></script>
```

- [ ] **Step 3: ④自前の isHyoukaMonth を削除**

`ケアマネ送付チェックリスト.html` 626行目付近の以下の関数定義を**丸ごと削除**（shared.js §I の正準版を使うため）:

```javascript
function isHyoukaMonth(planStart, ym) {
  // ...（既存の④ローカル実装。planStart,ym の2引数版）...
}
```
※ `isMeasureMonth`(639行) は体測用で正準化対象外。**残す**。

- [ ] **Step 4: fetchAll で keikakusho の users を保持**

`ケアマネ送付チェックリスト.html` 854行目:

```javascript
  allData.keikakushoRecords = (keikakushoRes && keikakushoRes.records) ? keikakushoRes.records : [];
```
の**直後**に追加:

```javascript
  // 個訓 計画開始月/計画月数（planStart, planMonths）= ①と同源。評価月判定の正準化に使う
  allData.keikakushoUsers = (keikakushoRes && keikakushoRes.users) ? keikakushoRes.users : [];
```

- [ ] **Step 5: buildTargets の個訓評価判定を正準関数に差し替え**

`ケアマネ送付チェックリスト.html` 924-926行目:

```javascript
    } else if (u.care === 'kaigo') {
      // 個訓 目標達成評価: 評価月該当判定とPDF/印刷送付状況
      const isHyouka = isHyoukaMonth(planStart, ym);
```
を以下に置換（①と同源の planStart/planMonths を引いて正準判定）:

```javascript
    } else if (u.care === 'kaigo') {
      // 個訓 計画/評価: ①と同源(getKeikakushoYear users)の planStart/planMonths で正準判定。
      // これで計画月・評価月とも①の isPlanMonth/isHyoukaMonth と件数一致する（完了条件2）。
      const kUser = (allData.keikakushoUsers || []).find(ku => ku.userId === u.name) || {};
      const kPlanStart = kUser.planStart || '';
      const kPlanMonths = kUser.planMonths || 3;
      const [hY, hM] = ym.split('-').map(Number);
      const isHyouka = isHyoukaMonth(kPlanStart, kPlanMonths, hY, hM);
      // 計画月も①同源に正準化（従来の planStart===ym 単発判定を置換）。
      // ※挙動変化: 3ヶ月毎の計画月に個別計画書チップが出る（=①と同基準・運用上より正確）。
      const hasNewPlanK = isPlanMonth(kPlanStart, kPlanMonths, hY, hM);
```
※ 直後の既存行 `const [hY, hM] = ym.split('-').map(Number);`(927) は上で宣言済みになるため**重複削除**する（残すと `const` 二重宣言エラー）。
※ さらに、この kaigo 側 `targets.push({...})` の `hasNewPlan: hasNewPlan,`(938) を **`hasNewPlan: hasNewPlanK,`** に置換する（kaigoの計画月を①同源に）。shien側の push は変更しない（要支援の planStart は廃止加算の名残のため hasNewPlan=false のまま）。

- [ ] **Step 6: 構文確認（ブラウザで起動しコンソールエラーが無いこと）**

Run（手元の簡易確認）: `node -e "require('fs').readFileSync('ケアマネ送付チェックリスト.html','utf8')" && echo OK`
さらにブラウザで `ケアマネ送付チェックリスト.html` を開き、DevToolsコンソールに **SyntaxError/ReferenceError が出ない**こと、対象者が描画されることを目視確認。
Expected: エラー無し・従来通り事業所別に対象者が出る。

- [ ] **Step 7: Commit**

```bash
git add "ケアマネ送付チェックリスト.html"
git commit -m "refactor(soufu): ④の個訓評価判定をshared.js正準関数＋①同源(planStart/planMonths)へ集約"
```

---

## Task 3: 通所モニタリング（getMonitoringYear）取込と精緻化

**Files:**
- Modify: `ケアマネ送付チェックリスト.html`（fetchAll・buildTargets）

**狙い:** ③getMonitoringYear を取込み、要支援/事業対象の通所モニタリングに「最終評価月＝サイン要(✍)」を反映。事業所キー `caremaneOffice` を `office` へ名寄せ（フォールバック）。

- [ ] **Step 1: fetchAll に getMonitoringYear を追加**

`ケアマネ送付チェックリスト.html` の Promise.all 配列（831-846行）末尾、`getKeikakushoYear` の要素の後にカンマ区切りで追加:

```javascript
    ,
    // 通所モニタリング: 年度の利用者(planStart,finalEvalMonth,caremaneOffice)と記録
    jsonp(YAWARAGI_BOARD_API + '?action=getMonitoringYear&year=' + fiscalYear)
      .catch(() => ({ ok: false, users: [], records: [] }))
```
さらに分割代入（831行 `const [...] = await Promise.all([`）の受け側末尾に `, monitoringRes` を追加。
そして 854行付近の代入群の末尾に追加:

```javascript
  // 通所モニタリング年度データ（users: planStart/finalEvalMonth/caremaneOffice, records: 送付日）
  allData.monitoring = (monitoringRes && monitoringRes.ok) ? monitoringRes : { users: [], records: [] };
```

- [ ] **Step 2: buildTargets で通所モニの finalEvalMonth インデックスを作る**

`ケアマネ送付チェックリスト.html` buildTargets 内、口腔Set構築の後（879行の後）に追加:

```javascript
  // 通所モニタリング: userId → finalEvalMonth（最終評価月＝サイン要）索引。caremaneOffice名寄せ用も保持
  const monUsers = (allData.monitoring && allData.monitoring.users) || [];
  const monFinalEvalMap = {};
  const monOfficeMap = {};
  monUsers.forEach(mu => {
    if (mu.finalEvalMonth) monFinalEvalMap[mu.userId] = mu.finalEvalMonth;
    if (mu.caremaneOffice) monOfficeMap[mu.userId] = mu.caremaneOffice;
  });
```

- [ ] **Step 3: shien の target に通所モニ情報を付与**

`ケアマネ送付チェックリスト.html` の `u.care === 'shien'` 側 `targets.push({...})`（910-923行）に、プロパティを追加（`oralPlanSent` の後にカンマ追加して）:

```javascript
        oralPlanSent: oralSentSet.has(u.name),
        // 通所モニタリング: 要支援/事業対象は毎月対象。finalEvalMonth の月はサイン要(✍)
        needsMonitoring: true,
        monitoringSignRequired: (monFinalEvalMap[u.name] === ym),
        monitoringOffice: monOfficeMap[u.name] || ''
```

- [ ] **Step 4: 名寄せ正規化に通所モニのofficeも合流（フォールバック）**

`ケアマネ送付チェックリスト.html` 957行（targets正規化の行）の直前に追加:

```javascript
  // cmOffice未登録の利用者は通所モニの caremaneOffice をofficeフォールバックに使う（名寄せ前）
  targets.forEach(t => {
    if ((t.office === '(事業所未登録)' || !t.office) && t.monitoringOffice) t.office = t.monitoringOffice;
  });
```

- [ ] **Step 5: 起動して通所モニが壊れず描画されることを目視確認**

ブラウザで開き、DevToolsコンソールにエラーが無いこと、要支援者に通所モニ項目が出ること、`getMonitoringYear` がNetworkタブに **GET** で出ていることを確認。
Expected: エラー無し。getMonitoringYear が200で取得され、最終評価月の利用者にサイン要が出る。

- [ ] **Step 6: Commit**

```bash
git add "ケアマネ送付チェックリスト.html"
git commit -m "feat(soufu): 通所モニタリング(getMonitoringYear)取込・最終評価月サイン要・caremaneOffice名寄せ"
```

---

## Task 4: 事業所別⇄利用者別 切替トグル ＋ 通所近似の注記

**Files:**
- Modify: `ケアマネ送付チェックリスト.html`（controls HTML・render のグループ化・イベント）

**狙い:** 同じ `targets` を並べ替えるだけのビュー切替（再取得しない）。事業所別（既定）と利用者別。加えて通所が1年固定近似である注記を画面に出す（完了条件6）。

- [ ] **Step 1: controls にトグルと近似注記を追加**

`ケアマネ送付チェックリスト.html` 397行 `<label>対象月: <select id="monthSel"></select></label>` の直後に追加:

```html
  <label style="margin-left:12px;">表示:
    <select id="viewModeSel">
      <option value="office" selected>事業所別</option>
      <option value="user">利用者別</option>
    </select>
  </label>
```

`.controls` div の閉じ（414行 `<button id="resetBtn">` の後の `</div>`）の直前に注記を追加:

```html
  <div class="approx-note" style="flex-basis:100%;font-size:12px;color:#a05a00;margin-top:4px;">
    ※ 通所介護計画の周期は<strong>1年固定の近似</strong>表示です（ケアプラン期限連動はrebuild後に正確化）。
  </div>
```

- [ ] **Step 2: render の事業所別グループ化を viewMode 対応に差し替え**

`ケアマネ送付チェックリスト.html` 1012-1035行（`// 事業所別グループ化` から `sortedOffices` 構築まで）を以下に置換:

```javascript
  // ビューモード（事業所別 / 利用者別）。同じ targets を並べ替えるだけ・再取得しない。
  const viewMode = (document.getElementById('viewModeSel')?.value) || 'office';

  // グループ化: office=事業所キー / user=利用者名キー（1人1グループ・ふりがな順）
  const grouped = {};
  let sortedOffices;
  if (viewMode === 'user') {
    targets.forEach(t => { grouped[t.name] = [t]; });
    const furiOf = (name) => {
      const u = allData.users.find(uu => uu.name === name);
      return (u && u.furigana) ? u.furigana : name;
    };
    sortedOffices = Object.keys(grouped).sort((a, b) => furiOf(a).localeCompare(furiOf(b), 'ja'));
  } else {
    targets.forEach(t => {
      if (!grouped[t.office]) grouped[t.office] = [];
      grouped[t.office].push(t);
    });
    // 人数多い順 ＋ 同一敷地ペア隣接（従来挙動）
    const baseSorted = Object.keys(grouped).sort((a, b) => grouped[b].length - grouped[a].length);
    sortedOffices = [];
    const placed = new Set();
    for (const office of baseSorted) {
      if (placed.has(office)) continue;
      sortedOffices.push(office);
      placed.add(office);
      const partner = getPairPartner(office);
      if (partner && grouped[partner] && !placed.has(partner)) {
        sortedOffices.push(partner);
        placed.add(partner);
      }
    }
  }
```
※ 以降の描画ループは `grouped` / `sortedOffices` をそのまま使う。利用者別では「グループ見出し＝利用者名」になり、各ブロックに1人だけ並ぶ（事業所名は行内に従来表示）。連絡先・送付方法ルックアップは各 target の `office` を引くので両モードで機能する。

- [ ] **Step 3: viewModeSel の変更で再描画（再取得しない）**

`ケアマネ送付チェックリスト.html` の monthSel の change ハンドラ付近（既存のイベント登録箇所、`monthSel` の `addEventListener('change'` を grep で特定）に倣い、初期化処理の中へ追加:

```javascript
  document.getElementById('viewModeSel').addEventListener('change', render);
```
※ `render()` のみ呼ぶ（`fetchAll()` は呼ばない）＝再取得なし・並べ替えのみ。

- [ ] **Step 4: 動作確認（切替・フィルタ・get-only）**

ブラウザで開き:
- 「表示」を 事業所別⇄利用者別 で切替→ 画面が並べ替わる（**Networkに新規リクエストが飛ばない**＝再取得なし）。
- 未提出フィルタ（filterFinalizableOnly 等）が両モードで効く。
- Networkタブで切替時に update*/set*/delete* が**一切飛ばない**。
Expected: 切替で並べ替えのみ。GET含め新規リクエスト無し。エラー無し。

- [ ] **Step 5: Commit**

```bash
git add "ケアマネ送付チェックリスト.html"
git commit -m "feat(soufu): 事業所別⇄利用者別 切替トグル（並べ替えのみ）＋通所1年固定の近似注記"
```

---

## Task 5: 完了条件の実測検証（手動・ネットワーク観測）

**Files:** なし（検証のみ）

- [ ] **Step 1: 集約画面の確認（完了条件1）**

当月で `ケアマネ送付チェックリスト.html` を開く。取得→名寄せ→事業所別に集約された画面が出ることを確認。

- [ ] **Step 2: ①との件数突合（完了条件2）**

`個別機能訓練計画書チェック.html` を当月で開き「今月計画対象」「今月評価対象」の人数を控える。④の当月で個訓 計画/評価の対象者数が①と**一致**することを確認。
※ 不一致なら Task 2 の planStart/planMonths 同源化を見直す（②に記録：差分が出た利用者名）。

- [ ] **Step 3: ③caremaneOffice の合流確認（完了条件3）**

cmOffice未登録だが通所モニ対象の利用者が、`caremaneOffice` 正規化により正しい事業所カード（利用者別では正しいoffice表示）に入ることを確認。

- [ ] **Step 4: トグル・フィルタ（完了条件4）**

事業所別⇄利用者別 切替、未提出フィルタが効くことを確認。

- [ ] **Step 5: get-only の確認（完了条件5）**

DevTools Network を録画した状態で、起動・月切替・ビュー切替・フィルタ操作を一通り行い、`update*` / `set*` / `delete*` を含む**書き込み系リクエストが1件も飛んでいない**ことを確認（GAS POST が皆無であること）。

- [ ] **Step 6: 通所近似の注記（完了条件6）**

画面に「通所は1年固定の近似」注記が表示されていることを確認。

- [ ] **Step 7: 検証結果を記録**

完了条件1-6の結果（特に Step 2 の突合件数）を、このプラン末尾またはコミットメッセージに残す。本番配信（④HTML／shared.js）は**社長承認のうえ別手順**（このプランでは push しない）。

---

## 次フェーズ（このプランのスコープ外・記録）

- ①個訓・②口腔を shared.js §I 参照へ切替（本番運用アプリの配信・回帰を伴う＝GAS書き込みタスク直前の関所で）。
- 通所 `min(ケアプラン, 1年)` 化（ケアプラン有効期限のデータ源新設が前提）。
- 実績送付状態の localStorage→GAS/Drive 同期（複数端末共有）。
- 口腔の実施記録／計画書アプリ分割（当初の別タスク）。
