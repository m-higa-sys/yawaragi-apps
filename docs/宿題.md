# 宿題（横断バックログ）

セッションを跨いで失われないように、着手待ちの残タスクをここに集約する。

- **単一の正**：残タスクはこのファイルに書く。個別機能に閉じた深い検討は専用ファイルへ切り出し、ここからリンクする。
- **完了したら**：チェックを付けて残すのではなく、行ごと消して commit する（履歴に残る）。
- ⚠️ **このリポジトリは PUBLIC。** scriptId・deploymentId・PIN・トークン・URL断片などの認証情報は書かない。「保管方式の見直し」のような抽象表現に留める。

## 関連する個別ファイル

- [docs/2026-07-14-shift-admin-auth-followup.md](2026-07-14-shift-admin-auth-followup.md) — シフト希望の管理者認可まわりの宿題（JSONP→POST化・アクセスログ・enforce維持・PIN強度）

## 確認済み（再調査不要）

- **管理者API認可の enforce は本番で ON**（確認日 2026-07-21・社長が本番 Script Properties を直接目視）。
  段階導入は完了とみなす。無認証で管理者API（getAllDataAdmin / getBossRests 等）を叩ける穴は無い。
  → follow-up の「enforce を OFF に戻さない」項目は**達成済み**。今後「未確認」扱いで再調査しないこと。
  （実際の値・トークン類は本ファイルに書かない。ON である事実のみ記録）

- **スタッフ画面のプライバシー（案A）は本番反映済み**（反映日 2026-07-21・デプロイ @24・コード.js＋画面.html）。
  到達点の定義＝「スタッフ用APIの応答から**他スタッフの氏名を除去**する。ただし次の2つは**意図的に残す**」：
    ① `staff`（名簿）… ログイン時の名前選択に必須。元から無認証 getStaff で公開されている情報。
    ② `notifications`（本人宛の被り通知）… 「◯◯さんと相談してください」と当事者に調整を促す能動通知。
       相手名が無いと機能しないため実名を残す。本人が当事者の被りに限定される（getNotifications が本人宛だけ返す）。
  → **「氏名ゼロ」ではない。** wishes/confirmations/wishCounts/absences には氏名が無いことを本番APIで実測済み。
    次のセッションが notifications や staff の氏名を見て「漏れている」と誤検知して直しにいかないこと（案Aの仕様）。

---

## シフト希望まわり

- [ ] shift.html に送信中ロックを入れる（二重タップ対策）
      ※shift.html は版ゲート未導入。反映時は職員へ Ctrl+F5 の依頼が必要。
        シフト希望の提出期限日は避ける
- [ ] 外せない予定シートの重複3行の掃除
      ※id が配列インデックス依存のため、削除すると以降の id がずれる。
        removeAbsence の実装確認と、id の安定化（UUID列等）の検討が先
- [ ] shift.html への版ゲート導入（Ctrl+F5 依頼を恒久的に不要にする）
- [ ] シフト希望スプレッドシートにおける認証情報の保管方式と共有範囲の見直し
- [ ] getAbsences のAPIレスポンスに登録日時を含める（調査をAPIだけで完結させるため）

## リポジトリ衛生

- [ ] PUBLIC repo にコミット済みの scriptId 4件の棚卸し。
      あわせて gitleaks のルールに scriptId が含まれていない点を見直す
- [ ] gas/ 直下の単体 .gs 11件が clasp管理外である件。
      編集しても本番に届かず、本番を直しても repo が古くなる片方向ドリフトが起きる。
      恒久策は clasp管理下への移行。触る予定が立った時点で1件ずつ行う
- [ ] gas/shift-kibou/画面.html と shift.html の乖離を解消する（**送信ロック分のみ残存**）
      2026-07-20 の送信中ロック（withSubmitLock）を shift.html にのみ入れたため乖離した。
      2026-07-21 のプライバシー修正（案A・wishCounts 等）は**両ファイルに入れたので同期済み**。
      残る差分は withSubmitLock（画面.html は 0 箇所・shift.html は 9 箇所）。
      職員が使うのは GitHub Pages 版（shift.html）で、画面.html は GAS の URL を
      直接開いた場合のみ到達。サーバ側 LockService が効くため実害は限定的だが、
      「そこにあるが本番ではないファイル」は取り違えを生む（gas_シフト希望.gs と同じ構造）。
      選択肢：(a) 送信ロックを画面.html にも同期する (b) 墓標化して正本の場所を明記する

## テスト環境

- [ ] テスト環境の依存解決が repo 外に依存している
      jsdom を /tmp/node_modules に配置しているため、
      /tmp のクリーンアップで消えて次セッションでテストが動かなくなる。
      既存のDOMテストは C:/tmp/node_modules を見る Windows 前提の記述で、
      Mac では動かない状態だった（2026-07-20 に両対応へ修正）。
      恒久策は package.json での devDependency 管理。
      repo に package.json を置くか否かは方針判断が必要。

---

## 背景メモ

- 2026-07-20：シフト希望GASの登録競合を LockService で根治し本番へ反映（デプロイ @23）。
  同日、本番未接続だった `gas/gas_シフト希望.gs` を削除し、正本一覧を CLAUDE.md に明記した。
  上の「gas/ 直下の単体 .gs」の宿題は、この取り違えが他ファイルでも起こり得るという指摘から出ている。
