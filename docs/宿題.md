# 宿題（横断バックログ）

セッションを跨いで失われないように、着手待ちの残タスクをここに集約する。

- **単一の正**：残タスクはこのファイルに書く。個別機能に閉じた深い検討は専用ファイルへ切り出し、ここからリンクする。
- **完了したら**：チェックを付けて残すのではなく、行ごと消して commit する（履歴に残る）。
- ⚠️ **このリポジトリは PUBLIC。** scriptId・deploymentId・PIN・トークン・URL断片などの認証情報は書かない。「保管方式の見直し」のような抽象表現に留める。

## 🔴PUBLIC repo に実患者名簿が直書き（PII露出・要移設）

- [ ] `gas/yawaragi-board/コード.js` の `SHIEN_SOKUTEI_PAPER_SEED`（紙台帳投入元・測定アンカー用）に**実患者 約60名の氏名が直書き**され、PUBLIC repo に露出している。
      これは「実名4件」と当初思われたものの実態＝**名簿1本**。名前が名寄せ/投入の実データ（挙動依存）のため、コード上で単純匿名化すると測定投入が壊れる。
      **要対応（別GO・設計要）**：名簿を**非公開の Sheet か Script Properties へ移設**し、コードからは参照のみにする。合わせて既存の匿名化据え置きリスト（docs/PII匿名化-据え置きリスト.md）と整合を取る。
      **済**：ロジック非依存の**コメント実名3行**（バウンス通知例／字体正規化例／利用終了1名）は 2026-07-26 に匿名化（挙動不変）。
      発覚：2026-07-26、kunren整流後の PII 実測時。

## 🔴本番 board GAS に認証情報が平文で直書き（Script Properties 化・要移設）

- [ ] 本番 board GAS の `コード.js`・`applauncher-mapping-core.js` に**認証情報が平文で直書き**されたまま生存している（2026-07-27 の clasp pull 実測で確認）。
      これがあるため repo 側からの素の push ができず、本番反映のたびに「clasp pull → additive適用 → push」の**現物ステージング方式が必須**になっている（上記🟡の不変ルールの根拠）。
      さらに作業用ステージングにも平文が落ちるため、**反映のたびに削除を手作業で担保**する必要があり、消し忘れがそのまま流出に直結する。
      隔離環境では pre-commit hook が効かない穴もあり、防御が運用手順頼みになっている。
      **要対応（別GO・設計要）**：認証情報を **Script Properties へ移設**し、コードからは `PropertiesService` 経由の参照のみにする（秘密情報ハードルール1に準拠）。
      移設後は現物ステージング方式を廃止でき素の push に戻せる＝運用側の事故要因がまとめて消える。
      発覚：2026-07-24 の伝達ボード反映設計時。2026-07-27 の反映で平文の生存を再確認（差分ゼロ＝無傷のまま反映）。

## 🔴clearDengonTestRows が伝達ボードの実データを全消しする（封印中・使用禁止）

- [ ] `gas/yawaragi-board/コード.js` の `clearDengonTestRows` は id が `db_` で始まる行を**全削除**するが、
      `addDengonMessage` は**実投稿にも `db_` + timestamp を採番**している（同ファイル内）。
      結果、コード内コメントの「実データ＝db_以外は保護」は**成立しておらず**、1回の実行でスタッフの伝達が全部消える。
      残るのは furikae-funou-\* / kunren-hold-\* などシステム自動生成行のみ。
      同じ前提に立つ `deleteDengonMessage` の db_ ガードも実データを弾けていない（1件指定なので被害は限定的）。
      いずれも `doGet` の action として外部から叩ける配線が入っている。
      **暫定対応**：2026-07-27 に**封印**（使用禁止）。テスト行の掃除はスプレッドシート「伝達ボード」での目視削除に切替。
      **要対応（別GO）**：真のテスト行だけを識別できる印（テスト専用の id プレフィクス、またはテストフラグ列）を導入し、
      `clearDengonTestRows` をその印に基づく削除へ作り直す。`deleteDengonMessage` のガードも同じ基準へ揃える。
      発覚：2026-07-27、伝達ボード社長宛通知の本番反映時に、後始末手順を実コードで裏取りして判明（実行前に発見・被害なし）。

## 🟡本番kunren（計画書期限）完成の残り（バックエンド整流は済）

- [ ] バックエンド（doGet4 action＋kd*群 約186行）は 2026-07-26 に master へ整流済み（commit a20f1e2）＝**素の master push で本番から消える地雷は解消**。
      **残り（別GO・クロ設計）**：枝 `feat/kunren-deadline` の **UI（個別…html）・中止者除外(statusCol)・kunren-name-normalize.js・テスト** が未マージ＝機能未完成。
      本番には今も「実名コメント」「UI未配線」「kunRecords 旧行」が残る（本番PII是正は上項＋次回 clasp pull→additive→push で反映）。
      **不変ルール**：個訓/board GAS の本番反映は必ず「clasp pull→additive適用→push」（素の master push 禁止）。

## 🟡計画書送付（keikaku_sent_date）を記録する画面が無い（2026-07-30）

- [ ] **送付アプリ（ケアマネ送付チェックリスト.html）に「計画書の送付」を足す。**
      2026-07-30 の社長決定で役割を分けた。個別機能訓練計画書チェックアプリは
      「計画書を作ったか／評価を作ったか」だけを管理し、ケアマネ送付は送付アプリの担当にした。
      これに伴い個訓アプリから「提出」バッジと送付の記録操作を撤去した（commit ad52d1f）。
      **実測での現状**：送付アプリは**評価の送付**（hyouka_pdf_date / hyouka_print_date）は
      📄PDF／🖨印刷 のピルで記録できるが、**計画書の送付（keikaku_sent_date）は未実装**。
      そのため計画書送付を記録する画面が、いまどのアプリにも無い。
      **足せる状態にはしてある**：シートの列・GAS の getKeikakushoYear（返す）/ updateKeikakusho
      （書ける）はいずれも無改修で残置。送付アプリ側にUIと updateKeikakusho 呼び出しを足すだけ。
      あわせて朝の報告の督促（morningDigest の safe('keikakushoSoufu')）も停止済み
      （件数0・空配列を返す形。getKeikakushoUnsubmitted_ 本体とセクションキーは残置）。
      止める直前の実測値は 計画書 未提出38件・評価 未提出22件。
      **戻す場合**：safe('keikakushoSoufu') の中身を `return getKeikakushoUnsubmitted_();` に戻す
      （実質1行）→ clasp push + deploy。データを1バイトも消していないので件数はそのまま復活する。

## 🟡個訓計画書：前々月以前の日付だとアプリ「済」・ボード「未」が残る（狭い穴・現時点0名）

- [ ] **発生条件**：個訓計画書の該当月ノードに入っている `keikaku_date` が、**その月の前々月以前**（または翌月以降）の日付のとき、
      アプリは済に見えるがボードは未に出す。原因は済判定の基準がアプリとボードで違うこと。
        - アプリ（個別機能訓練計画書チェック.html）：`keikaku_date` が**非空なら済**（月を問わない。`kbBadgeObj`／`updateStats`）
        - ボード（month-board-core.js）：`_mbFieldDoneWorkMonth_` で**当月 or 前月**のみ済
      2026-07-31 の「偽の未」是正で**前月付け**は解消したが（`_mbFieldDone_`→`_mbFieldDoneWorkMonth_`）、
      前々月以前は基準がずれたまま残っている。
      **影響範囲**：現時点で該当**0名**（2026-07 実測 kunPlan 対象16／済16／未0）。
      アプリの `kobetsuCycleAt` は1ヶ月しか前倒ししない設計なので、正当な運用では前々月の日付は発生しない。
      出るとすれば入力ミス（年の打ち間違い等）か、planStart を後ろへ動かして旧データが残った場合。
      **今の挙動はむしろ検知として機能する**ため、急いで塞ぐ必要はない。
      **恒久策の候補（案C）**：アプリ側の済判定も「当月 or 前月」に寄せて単一の正にする。
      ただしアプリ側は表示と入力の両方に効くため、影響確認のうえ別GOで実施する。
      判定ロジックそのもの（planStart／isPlanMonth／isHyoukaMonth）は不変で足りる。
      **kunEval（`tasseido_date`）は対象外**：評価に前倒し運用は無く（アプリの `showEval` は自セル判定・月ずらし無し）、
      在月判定のまま据え置きが正。現挙動は `scripts/test-month-board-workmonth.js` の D1〜D6 で固定済み。
      発覚：2026-07-31、朝報告「個訓計画書 13名 未完了」の誤報調査時。

## 🟡ケアマネ送付チェックリストの個訓計画書チップが当月主義のまま（軸不揃い）

- [ ] `ケアマネ送付チェックリスト.html:1088` の `hasNewPlanK = isPlanMonth(kPlanStart, kPlanMonths, hY, hM)` が
      **計画期間の開始月**でチップを出している（当月主義）。
      2026-07-31 に月次ボードの kunPlan を**作業月主義**（v1.2 §1-3 前月準備の原則）へ寄せたため、
      送付アプリだけが1ヶ月遅い軸で残った。`teishutsu.html:319-322` と月次ボードは作業月主義で揃っている。
      **実害**：送付チップが出るのは計画期間の開始月なので、「前月中にケアマネへ渡す」という
      v1.2 §1-3 の運用に対して1ヶ月遅れて出る。ボードが督促を終えた後にチップが出る形になる。
      **要対応（別GO）**：`isPlanMonth(..., 翌月)` へ寄せる。判定関数（shared.js）は不変更で、
      呼び出し側で渡す年月を変えるだけ（月次ボードと同じ手法）。年跨ぎの扱いを忘れないこと。
      発覚：2026-07-31、月次ボードの軸シフト実装時の影響調査。

## 🟡個訓アプリの shared.js?v= が version.txt と1ヶ月ズレている（既存ドリフト）

- [ ] **`個別機能訓練計画書チェック.html:704` が `shared.js?v=2026-06-26b` を指したまま。**
      2026-07-31 実測: `version.txt` = `2026-07-30-02`、`genba.html` 等は `shared.js?v=2026-07-30-02`。
      個訓アプリだけ **1ヶ月以上古いクエリ**で shared.js を要求している。
      **仕組み上の理由**：`scripts/bump-app-version.js` の `TRACKED` は `['version.txt', 'genba.html']` だけで、
      個訓アプリの `?v=` は版上げスクリプトの同期対象に入っていない（手で入れた固定値のまま）。
      **実害の見立て**：版ゲート（`<head>` の version.txt ポーリング → `?v=` 付きで location.replace）は
      HTML本体には効くので、個訓アプリ自体は最新版に切り替わる。ズレているのは **shared.js のキャッシュキー**だけ。
      ただし shared.js を更新しても個訓アプリだけ古い shared.js を掴み続けうる（判定関数の共有元なので影響は小さくない）。
      **要対応（別GO）**：`bump-app-version.js` の同期対象に個訓アプリを足すか、
      版ゲートと同じ仕組みで `?v=` を動的に付けるか。どちらも 🔢版を握る作業なので単独GOで。
      発覚：2026-07-31、温存バグ修正で同ファイルを触った際の実測。

## 🟡isPlanMonth / isHyoukaMonth は planMonths=3 以外だと1回きりで周期が止まる（未修正）

- [ ] **`planMonths` が 3 以外のとき `isPlanMonth` は `diff === 0` しか返さない**（`shared.js:441`）。
      実測（2026-07-31）:
      ```
      planMonths=3  計画月: 2026-01, 04, 07, 10, 2027-01 …（周期する）
      planMonths=2  計画月: 2026-01 のみ（以降なし）
      planMonths=1  計画月: 2026-01 のみ（以降なし）
      ```
      `isHyoukaMonth` も同様（`diff === (L-1)` または `diff === -1` の1回きり）。
      **現時点の実害は0名**（稼働中の要介護は全員 planMonths=3）。
      2026-07-31 に**画面から計画月数を変更する経路を塞いだ**ので、新たな変則値は増えない
      （`個別機能訓練計画書チェック.html` の select を表示専用テキストへ変更）。
      **判定ロジック自体は未修正のまま残る。**
      **修正時の注意**：定義は **2箇所**にある。`shared.js:431-441`（正本）と
      `gas/yawaragi-board/session-board-judges.js:10`（GAS用の逐語コピー・
      `scripts/test-session-board-judges.js` の DG3 で byte一致 drift を検知）。**必ず両方を同じ内容に**する。
      修正自体は実質1行だが、**`feat/kunren-deadline`（未マージ）が同じ周期判定に触れており衝突する**ため、
      そちらの処遇（master反映するか破棄するか）を決めてから着手する。
      発覚：2026-07-31、planStart 保持要否の調査時。

## 🟡月次ボードの軸を 2026-07-31 に変更した（過去月の数字と単純比較しないこと）

- [ ] **2026-07-31 より前に出した月次ボード／朝報告の「個訓計画書 対象/済/未」は旧軸（当月主義）の数字。**
      同日以降は作業月主義（ボード月の翌月が計画期間の開始月なら、そのボード月が対象）。
      **同じ月を指していても、軸変更日を挟むと数字が変わる。**記録や議事メモと突合するときは
      「いつ出した数字か」を必ず確認する。実測の変化（本番データ・2026-07-31時点）:

      | 月 | 区分 | 旧軸 対象/済/未 | 新軸 対象/済/未 |
      |---|---|---|---|
      | 2026-06 | kunPlan | 13/13/0 | 16/16/0 |
      | 2026-06 | kunEval | 17/11/6 | 16/11/5 |
      | 2026-07 | kunPlan | 16/16/0 | 20/19/1 |
      | 2026-07 | kunEval | 21/21/0 | 20/20/0 |

      kunEval が各月1名減っているのは「幻の督促ガード」（diff=−1＝planStart の前月は
      評価すべき前サイクルが無いので除外）を同時に入れたため。アプリ側
      `個別機能訓練計画書チェック.html:1323-1324` の `beforeStart` と同じ意味に揃えた。
      **過去月を遡って見ると、旧軸では「済」だった月に「未」が出ることがある**（前月中に
      作り終えていなかった分が可視化されるため）。これは誤報ではなく検知精度の向上。

## 🟡スプレッドシート「利用者台帳」(1blasas…) のタイムゾーンが UTC−7（JSTでない）

      2026-07-30 実測。スクリプト側 appsscript.json は `Asia/Tokyo` だが、
      **保存先スプレッドシートの TZ 設定は UTC−7**。両者が別設定であることを見落とすと
      調査が16時間ずれる（実際に未実施報告ボタンの調査で2回つまずいた）。
      校正手順：自分でPOSTを1発打ち、HTTP応答ヘッダの Date と access_log の
      timestamp を突合する（`JST = セル値 + 16h`）。
      裏付け：同一10,054行を時刻帯ヒストグラムに落とすと、セル値では 01時=908件・
      06〜10時=0件（デイサービスとしてあり得ない）、+16hでは 09時=1550件ピークで
      07時立ち上がり・22時以降0件になる。
      影響範囲：Date型で書かれた全シートの日付列。access_log の全時刻。
      **是正は単独案件。60シート・利用者台帳を含み、設定変更で全シートの日付解釈が
      16時間動くため、影響棚卸しを先に行う。**
      当面の回避策：日付は必ず `Utilities.formatDate(..., 'Asia/Tokyo', ...)` の
      **文字列**で書く（Date型オブジェクトをセルに渡さない）。読み取りは純関数で正規化する
      （実装例：`gas/yawaragi-board/undone-report-core.js` の `undoneNormalizeDateCell_`）。

## 🟡TOKEN_ENFORCE を ON にすると「今日できませんでした」ボタンが401で死ぬ

      2026-07-30 実測。sougei_nisshi.html の未実施報告POSTは `token` を送っていない
      （access_log 上 `token_status=missing` / `enforce=0` のため現在は通過）。
      Phase 2 でトークン付与の対象に含めること。同様に origin も送っていないため
      access_log 上は `(none)` になり、どのアプリからのPOSTか特定できない。

## 🟡本番GASの「配信中デプロイ版」との突合ができていない

      2026-07-30。board GAS のエディタ最新版と repo `gas/yawaragi-board/` は
      `diff -r` で差分ゼロを実測済み（Drive の Apps Script JSON エクスポート経由）。
      ただし `/exec` が実際に配信しているのは**バージョン固定のデプロイスナップショット**で、
      これがエディタ最新版と一致しているかは未確認（デプロイ一覧APIに到達できなかった）。
      `clasp login` 復旧後に `clasp list-deployments` で確認する。

## 🟡不可視の `#toast` が当たり判定を持っていた（他アプリも同型の可能性）

      2026-07-30 Chrome headless で実測。`.toast{position:fixed;top:60px;z-index:9999;opacity:0}`
      は **opacity:0 でもタップを吸う**。送迎日誌では未実施報告ボタンが報告済み状態
      （サブ文言ありで高さ75.4px）のとき、下端のタップが `#toast` に奪われていた
      （修正前 `下端=false / 当たった要素=#toast`、修正後 `下端=true / #undone-report-btn`）。
      同じ `.toast` パターンを持つ他アプリにも `pointer-events:none` が必要か棚卸しする。
      なお当初 `.controls`(sticky/z-index:100) を疑ったが、**実測では .controls が
      タップを奪う場面は無かった**（sticky が張り付く前にボタンが画面外へ抜ける）。
      CSSの幾何からの推測ではなく、必ず実機レイアウトで測ること。

## 🟡孤立ブランチ6本＋メイン外ワークツリー2件（処遇未決）

      2026-07-30 `node scripts/check-orphan-branches.js` 実測（基準 origin/master = 01d05b9）。
      origin/master 未マージのローカルブランチ:
        - backup/dengon-pre-rebase      (+1 / 最終 2026-07-24)
        - feat/haichi-2nin-reason       (+2 / 最終 2026-07-21)
        - feat/kunren-deadline          (+2 / 最終 2026-07-21)
        - fix/furikae-fold-kurikoshi    (+1 / 最終 2026-07-23)
        - fix/intake-save-verify        (+1 / 最終 2026-07-29) [WT: dev/wt-intake-save-verify]
        - fix/mb-fake-undone            (+1 / 最終 2026-07-27)
      メイン以外のワークツリー:
        - dev/wt-dengon-owner-option [feat/dengon-owner-option] **未コミット作業1件**（要中身確認）
        - dev/wt-intake-save-verify  [fix/intake-save-verify] クリーンだが master 未反映
      各項目を「master反映」「削除」「残す理由を記録」のいずれかへ倒す。
      特に wt-dengon-owner-option の未コミット差分は、本番未反映の作業の可能性があるため
      削除前に必ず中身を見る（2026-07-02 の「ブランチに孤立して修正済み扱い」事故と同型）。
      ※ feat/undone-report（本件）は master 反映後に worktree ごと掃除する。

## 🟡「未実施報告」4月1行目の1日ずれは原因未特定（端末日付説が唯一の生き残り）

      1行目: `date=2026-04-28` / `reportedAt=2026-04-29T14:28:11+09:00`（約1日ずれ）。
      **Date型書込説は否定済み** ― date セルの生シリアルは `46140.0` で小数部ゼロ＝日付のみ。
      `new Date()` の瞬間を書いたなら `46140.9362…`（04-28 22:28:11）になり小数部が残る。
      2行目は約1時間後に `46141.0`＝04-29 で、サーバ側の固定的なTZバグでは説明できない。
      **唯一の生き残り仮説＝端末日付説**：クライアントが文字列 `date:'2026-04-28'` を送り、
      Sheets が日付型セルへ自動変換した。`_undoneTodayDateStr()` は端末ローカル日付を返すため、
      端末時計がJSTでなければ 04-28 になり得る。どの端末だったかは特定できていない。
      2026-07-30 の修正で、サーバJSTと±1日を超えて食い違う端末日付は**書き込まずに拒否**し
      画面へ理由を返すようにした（再発時は黙って通らず、端末日付と施設日付が画面に出る）。
      それ以前の1件については、端末が特定できないため原因確定は不可能。

## 🔴送迎日誌の「添乗者」欄が30秒ごとに入力前へ戻る（版ゲートとは無関係の既存バグ）

      2026-07-30 実ブラウザ（Chrome headless・ローカルコピー・本番通信は fetch/sendBeacon 両方ブロック）で実測。
      `loadDate()` は出勤＆送迎表データ(`yawaragi_daily_ops`)があると `snapshotFromOps()` で
      dayData を作り直すが、復元するのは **remarks / absent / times の3つだけ**。
      `riders` は `snapshotFromOps` が `routes.map(r => r.rider)` で上書きするため、
      送迎日誌で入力した添乗者は毎回消える。
      `scheduleSave()` → localStorage には正しく入っている（実測 `riders=["添乗丁","添乗乙"]`）のに、
      `loadDate()` 直後の画面は種データに戻る（実測 DOM="添乗甲"）。
      発火経路は**リロードだけではない**: 30秒の自動更新（`startAutoRefresh` → cloudLoad成功 → loadDate）と
      `visibilitychange`（画面復帰時）でも同じ経路を通る。
      ＝現場で添乗者を入力しても、最大30秒で入力前に戻る。
      是正案（未実装・要判断）: `loadDate()` の復元対象に oldRiders を追加する
      （times と同じく「ルート数が一致する場合のみ復元」でよいか要確認）。
      ※本件は 2026-07-30 の未実施報告ボタン修理のスコープ外。混ぜずに単独で直す。

## 🟡送迎日誌の時刻欄は「フォーカスを外す前のリロード」で消える

      2026-07-30 実測。時刻入力は `oninput="formatTimeInput(this)" onchange="scheduleSave()"`（955行）で、
      **保存は change（フォーカス外し／確定）のときだけ**。他の欄（添乗者・特記事項は `oninput`）を
      触ると `collectInputs()` が DOM 全体を集めるので時刻も巻き込まれて保存されるが、
      時刻だけを入力してフォーカスを外さずリロードされると消える（実測: 入力 "9:47" →
      リロード前の localStorage times は種データ ["8:51",…] のまま → リロード後 DOM="8:51"）。
      `flushCloudSave()`（500行・beforeunload/pagehide）は `collectInputs()` を呼ばないため、
      離脱時にも DOM の未確定値は拾わない。
      是正案（未実装）: 時刻にも `oninput` で scheduleSave を掛ける、または flushCloudSave の先頭で
      collectInputs() を呼ぶ。

## 🟡「未実施報告」date 列が Date型になる（案B・厳密な文字列型にするなら別案件）

      2026-07-30 本番実測。reportUndone_ は Utilities.formatDate(..., 'Asia/Tokyo', 'yyyy-MM-dd')
      の**文字列**を appendRow に渡しているが、Sheets が日付型セルへ自動変換する。
      実測値: 生シリアル 46233.0（小数部ゼロ＝日付のみ・時刻成分なし）→ 復号 2026-07-30 00:00:00。
      既存4月2行（46140.0 / 46141.0）と同じ形。**日付のずれは発生していない。**
      読み取りは undoneNormalizeDateCell_（Date型/文字列/空を JST 日付へ正規化）が吸収し、
      冪等・取消・朝報告の窓判定すべてが本番で正しく動くことを実証済み（実害ゼロ）。
      2026-07-30 社長判断で **案A（現状のまま）** を採用。
      厳密に文字列型にする案B（setNumberFormat('@') またはアポストロフィ前置）は
      **列の書式変更＝構造変更**であり、既存4月2行の型も変わるため単独案件とする。
      着手するなら、シートTZ が UTC−7 である件（上記）と併せて設計すること。

## 🟡「今日できませんでした」ボタンの存続判断（2026-10月末に再評価）

      2026-07-30 に修理完了（それまで3ヶ月間、保存経路が存在せず0件）。
      社長の当初の意図は「不具合が多いから付けた」。使用データがゼロのため、
      消す判断をデータなしで下さない方針で **3ヶ月そのまま残して使われ方を測る** と社長決定。
      文言変更もしない（先に意味を決めると押し方を誘導するため保留）。
      2026-10月末に朝報告への出現件数を集計して、残すか消すかを判断する。
      未解決の論点：意味が二重になっている。
        シート名＝「未実施報告」／朝報の見出し＝「昨日できなかった業務」＝業務が終わらなかった
        社長の意図＝アプリが壊れて入力できなかった
      伝達ボードとの使い分けも未定。3ヶ月分の実データを見てから決める。
      不具合報告の導線としては弱点あり（押してから最大30秒待つ・LINEの方が速い）。

## 🟡個訓グリッド: 2026-06 より前の行を据え置く暫定ルール（KB_WORK_MONTH_FROM）

      2026-08-01 段階6-1（社長決定・案③）で、計画パートの列を「作業した月」に揃えた。
      配置ルール:「期間開始月が KB_WORK_MONTH_FROM(2026-06) 以降なら その1つ前の月の列に描く。
      それより前なら 期間開始月の列に描く。」
      定数の場所: 個別機能訓練計画書チェック.html の `const KB_WORK_MONTH_FROM = '2026-06';`
      （純関数 kbPlanMovesToPrevMonth が唯一の参照元。判定はこの1箇所に集約している）
      理由: 2026-06 が作業月主義（前月準備の原則・v1.2 §1-3）の運用開始月。それ以前の記録は
      「その月に作ってその月ぶんを出す」やり方だったので、後から並べ替えない、という社長判断。
      実測の裏づけ: 計画書作成日 76件のうち 2026-06 以降の行は 48件が前月実施（＝作業月と一致）。
      2026-04 の行 19件は全て当月実施で、前月へ寄せると実施月とも年度グリッドとも合わなくなる。
      ▼将来この定数を消せる条件:
        2026-05 以前の行が年度グリッドから完全に外れたとき（＝2027年度＝2027-04 以降しか
        表示しなくなったとき）。そのとき kbPlanMovesToPrevMonth は常に true になるので、
        関数ごと畳んで「常に前月へ寄せる」に単純化できる。
      ▼副作用（許容済み）: 境界の 2026-05 列だけ、1セルに計画パートが2本並びうる
        （5月の行＝据え置き ＋ 6月の行＝移動）。2026-08-01 の本番データで実測 2名。
        どちらの記録も捨てない側を選んだ結果。2027年度になれば自然に消える。

## 🟡個訓グリッド: 作業月と表示列が1ヶ月ずれる記録が5件残る（表示のみ・実害なし）

      2026-08-01 段階6-1 の実測。列＝作業した月に揃えたが、行の月＝期間の開始月であって
      「実施日の月」ではないため、その月のうちに作った記録は表示列が1ヶ月手前にずれる。
        2026-06 の行で実施日も 2026-06 … 2件 → 5月列に出る
        2026-07 の行で実施日も 2026-07 … 3件 → 6月列に出る
        計 5件（2026-08-01 時点・本番データ実測。表示対象50名ぶん。一致は 64件）
      これらは「前月に作る」運用に乗り切っていない記録で、実施月より1つ手前の列に出る。
      実害は表示位置だけ（記録・書込先・集計は正しい）。2026-08 の行は 20/20 が前月実施なので、
      運用が定着するにつれ自然に解消する。
      恒久策を採るなら「行ではなくバッジの実施日で列を決める」になるが、未記録のものに
      日付が無いため「▶ 準備」の置き場所が決まらない。現状は据え置きが妥当。

## 🟡個訓グリッド: 個訓シートに行が1つも無い人の測定記録シートの測定は表示されない

      2026-08-01 段階6-1（クロ決定・指示8）で、計画パートを立てる判定を
      「個訓シートの行データのみ」にした。測定記録シートだけを根拠に計画パートを立てると、
      書込先が「期間の開始月ではない行」になってしまうため（意図しない行への書込防止）。
      その結果、個訓シートに1行も無い人の測定記録シート由来の測定は、画面に出なくなる。
      2026-08-01 時点の本番実測では該当 0件（測定記録シートだけの4件は、いずれも同じ列に
      描かれる計画パートの測定✓として出ることを確認済み）。
      発生した場合の影響は表示のみ（測定記録シートのデータは無傷）。
      対処が要るなら「その月の自セルに、書込先＝自セルの月として計画パートを立てる」で戻せる。
      回帰テスト: scripts/test-kobetsu-grid-dom.js の S3b / S3c、
      scripts/test-kobetsu-label-refresh.js の C4 / C5 が現在の仕様を固定している。

## 🟢個訓の測定を測定管理アプリへ片寄せ済み（2026-08-01・巻き戻し方法つき）

      2026-07-28 の段階1で「要介護の測定も測定記録シートへ寄せる。片寄せは段階3で行う」と
      決めていたものを、2026-08-01 に実施した（社長決定・案A）。
      直接の理由: 個訓アプリから測定を入れても測定管理側の予定月(domain='sokutei')が
      追随せず、同じ人に測定の督促が出続ける。2026-08-01 時点の実害は1名だが、
      個訓から入力するたびに増える構造だった。
      撤去したもの（個別機能訓練計画書チェック.html のみ）:
        - saveMeasureFromDialog（測定日／測定者／出力者を個訓シート13〜15列目へ書く関数）
        - 入力欄 sokuteiDateInput / sokuteiByInput / outputByInput
      残したもの:
        - 測定の【表示】。バッジは2ソース和（個訓シート13列目 ∪ 測定記録シート）のまま。
          既存データ（2026-08-01 実測: 個訓シートにだけ測定日がある20名）は読み続ける。
        - 計画書ダイアログの案内（いまの測定状態＋「測定アプリを開く」リンク・別タブ）。
      ▼巻き戻し方法（GAS は1バイトも触っていないので、HTML を戻すだけで復活する）:
        GAS 側の受け口 updateKeikakusho の kuFieldAllowed（sokutei_date:13 / sokutei_by:14 /
        output_by:15）は撤去していない。個訓シート13〜15列目のデータも削除していない。
        よって saveMeasureFromDialog と3つの入力欄を戻せば、そのまま元の挙動に戻る。
        該当コミットを revert するのが最短。
      ▼予定月がズレている11名・測定記録が無い28名は【手で直さない】（社長判断 2026-08-01）。
        アプリを最近稼働させたばかりで、測定が入れば予定月は実施月＋3へ自動更新される。
        3〜4ヶ月で一巡し自然に揃う見込み。データの手修正はしないこと。

## 🟡月次ボード・セッションボードが測定をどこから読んでいるかは未調査

      2026-08-01 の片寄せは個訓アプリだけを対象にした。社長の方針は
      「測定アプリに一本化し、後のアプリ（個訓・月次ボード・セッションボード）は
      そこから反映する形がいい」。個訓は対応したが、残る2つは未確認。
      確認すべきこと:
        - month-board（gas/yawaragi-board/month-board-core.js）の測定判定が
          個訓シート13列目・測定記録シート・予定月シートのどれを見ているか
        - session-board（gas/yawaragi-board/session-board-core.js・sokuteiCycleMonths_ を持つ）が
          同様にどこを見ているか
        - 片方しか見ていない場合、個訓シートにしか測定日が無い20名と
          測定記録シートにしか無い4名で、どちらかが「未」に見えていないか
      いずれも読取のみの調査で判定できる。別GOで実施する。

## 🟡段階5（月次ボード・セッションボードの対象月を予定月へ）は未実施

      2026-08-01 に測定の【データ源】は直した（月次ボード・セッションボードが測定記録シートも
      読むようにした）。残っているのは【対象月の決め方＝位相】で、これが段階5。
      現状（2026-08-01 時点）:
        - 月次ボード   … isHyoukaMonth(u.planStart, u.planMonths, y, m)   month-board-core.js
        - セッションB … isHyoukaMonthFn(u.planStart, u.planMonths, y, m)  session-board-core.js
        - 個訓アプリ   … 予定月シート domain='kobetsu' ベース（段階4で移行済み）
      実測のズレ（2026-08）: 月次/セッションの対象12名 vs 個訓アプリの対象14名。一致は11名。
      ▼着手前に社長の設計判断が要る（これがボトルネック）:
        予定月シートには domain='sokutei'(112名) と domain='kobetsu'(52名) の2系統がある。
        要支援・事業対象者60名は計画書を持たないため kobetsu の行を持てない。
        よって「対象月をどちらの予定月で決めるか」を決めないと実装に入れない。
          案1: 測定は sokutei、個訓の計画書・評価は kobetsu（人によって参照先が変わる）
          案2: 要介護は kobetsu に寄せ、要支援は sokutei のまま（分野で分ける）
        いずれも要支援60名の測定がどう扱われるかを必ず論点に含めること。
      ▼データ源の修正（2026-08-01 実施）とは独立に進められる。まとめない方がよい理由:
        位相とデータ源は直交していて、まとめると督促の数字が変わったときに
        どちらが原因か切り分けられなくなる。

## 🟡測定管理アプリ(sokutei.html)だけ予定月の系統が違う（要介護11名で表示が食い違う）

      2026-08-01 の段階5で、月次ボード・セッションボード・朝報告の要介護の対象月を
      予定月シート domain='kobetsu'（＝計画書の予定）へ揃えた。社長判断:
      「測定・評価・計画書は1つの節目。ボードの同じ月に3つとも立つのが正しい」。
      ところが測定管理アプリ(sokutei.html)は要介護でも domain='sokutei' の予定月を
      使い続けている（今回は対象外・触っていない）。
      実測（2026-08-01）: 両方の予定月を持つ要介護52名のうち
        一致（sokutei = kobetsu − 1ヶ月）… 41名
        ズレ                          … 11名（−4ヶ月1／−2ヶ月5／−1ヶ月2／+1ヶ月2／+2ヶ月1）
      このズレている11名は、月次ボード／朝報告と測定管理アプリで「今月の対象か」の表示が
      食い違う。ズレの主因は初期投入(init)のまま実測定が無い6名と、手動の月タップ4名。
      測定が一巡すれば sokutei 側は実施月＋3で自動更新されるため自然に縮む見込み
      （社長判断でデータの手修正はしない）。
      ▼そろえるには社長の設計判断が要る（別GO）:
        予定月シートには sokutei(112名) と kobetsu(52名) の2系統がある。
        要支援・事業対象者60名は計画書を持たないため kobetsu の行を持てない。
        測定管理アプリを kobetsu へ寄せると、この60名の測定予定が拠り所を失う。
        「要介護だけ kobetsu、要支援は sokutei」と分けるか、器を1本化するかの判断が必要。

## 関連する個別ファイル

- [docs/2026-07-14-shift-admin-auth-followup.md](2026-07-14-shift-admin-auth-followup.md) — シフト希望の管理者認可まわりの宿題（JSONP→POST化・アクセスログ・enforce維持・PIN強度）

## 確認済み（再調査不要）

- **管理者API認可の enforce は本番で ON**（確認日 2026-07-21・社長が本番 Script Properties を直接目視）。
  段階導入は完了とみなす。無認証で管理者API（getAllDataAdmin / getBossRests 等）を叩ける穴は無い。
  → follow-up の「enforce を OFF に戻さない」項目は**達成済み**。今後「未確認」扱いで再調査しないこと。
  （実際の値・トークン類は本ファイルに書かない。ON である事実のみ記録）

- **スタッフ画面のプライバシー（案A）は本番反映済み**（反映日 2026-07-21・デプロイ @24・コード.js＋画面.html）。
  到達点の定義＝「スタッフ用APIの応答から**他スタッフの氏名を除去**する。ただし次の2つは**意図的に残す**」：
    ① `staff`（名簿）… ログイン時の名前選択に必須。元から無認証 getStaff で公開されている情報。
    ② `notifications`（本人宛の被り通知）… 「◯◯さんと相談してください」と当事者に調整を促す能動通知。
       相手名が無いと機能しないため実名を残す。本人が当事者の被りに限定される（getNotifications が本人宛だけ返す）。
  → **「氏名ゼロ」ではない。** wishes/confirmations/wishCounts/absences には氏名が無いことを本番APIで実測済み。
    次のセッションが notifications や staff の氏名を見て「漏れている」と誤検知して直しにいかないこと（案Aの仕様）。

---

## シフト希望まわり

- [ ] shift.html に送信中ロックを入れる（二重タップ対策）
      ※shift.html は版ゲート未導入。反映時は職員へ Ctrl+F5 の依頼が必要。
        シフト希望の提出期限日は避ける
- [ ] 外せない予定シートの重複3行の掃除
      ※id が配列インデックス依存のため、削除すると以降の id がずれる。
        removeAbsence の実装確認と、id の安定化（UUID列等）の検討が先
- [ ] shift.html への版ゲート導入（Ctrl+F5 依頼を恒久的に不要にする）
- [ ] シフト希望スプレッドシートにおける認証情報の保管方式と共有範囲の見直し
- [ ] getAbsences のAPIレスポンスに登録日時を含める（調査をAPIだけで完結させるため）

## リポジトリ衛生

- [ ] PUBLIC repo にコミット済みの scriptId 4件の棚卸し。
      あわせて gitleaks のルールに scriptId が含まれていない点を見直す
- [ ] gas/ 直下の単体 .gs 11件が clasp管理外である件。
      編集しても本番に届かず、本番を直しても repo が古くなる片方向ドリフトが起きる。
      恒久策は clasp管理下への移行。触る予定が立った時点で1件ずつ行う
- [ ] gas/shift-kibou/画面.html と shift.html の乖離を解消する（**送信ロック分のみ残存**）
      2026-07-20 の送信中ロック（withSubmitLock）を shift.html にのみ入れたため乖離した。
      2026-07-21 のプライバシー修正（案A・wishCounts 等）は**両ファイルに入れたので同期済み**。
      残る差分は withSubmitLock（画面.html は 0 箇所・shift.html は 9 箇所）。
      職員が使うのは GitHub Pages 版（shift.html）で、画面.html は GAS の URL を
      直接開いた場合のみ到達。サーバ側 LockService が効くため実害は限定的だが、
      「そこにあるが本番ではないファイル」は取り違えを生む（gas_シフト希望.gs と同じ構造）。
      選択肢：(a) 送信ロックを画面.html にも同期する (b) 墓標化して正本の場所を明記する

## テスト環境

- [ ] テスト環境の依存解決が repo 外に依存している
      jsdom を /tmp/node_modules に配置しているため、
      /tmp のクリーンアップで消えて次セッションでテストが動かなくなる。
      既存のDOMテストは C:/tmp/node_modules を見る Windows 前提の記述で、
      Mac では動かない状態だった（2026-07-20 に両対応へ修正）。
      恒久策は package.json での devDependency 管理。
      repo に package.json を置くか否かは方針判断が必要。

---

## 背景メモ

- 2026-07-20：シフト希望GASの登録競合を LockService で根治し本番へ反映（デプロイ @23）。
  同日、本番未接続だった `gas/gas_シフト希望.gs` を削除し、正本一覧を CLAUDE.md に明記した。
  上の「gas/ 直下の単体 .gs」の宿題は、この取り違えが他ファイルでも起こり得るという指摘から出ている。
