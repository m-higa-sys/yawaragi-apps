# LINEトークン差し替え手順（社長向け・2026-07-14）

新トークン（yawaragi社内・チャネルID 2009698871・本日再発行）を、各GASの
**Script Properties に社長が直接入力**して反映する手順。**値はここに書かない**。
1Passwordの値をコピペで貼る。**「プロパティを先に設定 → 後からコード反映」**の順が安全
（コードが先に動くと空トークンで失敗するため）。

---

## 対象と実キー名（ここが今回のキモ）

| # | GASプロジェクト | scriptId | 設定するプロパティ（実キー名） | コード側の状態 |
|---|---|---|---|---|
| 1 | **LINE通知（本番webhook）** | `1Pyoag…` | **`CHANNEL_ACCESS_TOKEN`** / `OWNER_USER_ID` | 既にProperties参照＝**コード変更不要** |
| 2 | **yawaragiボード** | `1pJN4vj…` | **`LINE_TOKEN`** / `OWNER_USER_ID` | repo修正済(`d4570cc`)。**要 clasp push** |
| 3 | **インク管理** | （SS紐付け・要確認） | **`LINE_TOKEN`** / `OWNER_USER_ID` | 要確認（getProperty版へ） |
| 4 | 複製 LINE通知（死んでる） | `1_n-UuWw…` | — | webhook無し・平文デッド値保持 → **削除推奨** |
| 5 | シフト希望 LINE実装版 | 未特定 | （未deploy） | 今回対象外 |

> ⚠️ キー名注意：**本番webhook(1Pyoag)だけ `CHANNEL_ACCESS_TOKEN`**、板・インクは `LINE_TOKEN`。
> `OWNER_USER_ID`（社長のLINE userID）は今回**再発行の影響を受けない**が、板・インクは
> 自プロジェクトのプロパティに未設定の可能性があるので、**1Pyoag のプロパティ画面から
> `OWNER_USER_ID` の値をコピーして板・インクにも貼る**（値はクロコを介さない）。

---

## 手順（この順で）

### ① 本番webhook（1Pyoag）＝最優先・これだけで通知は復活
1. `script.google.com/d/1PyoagHpIjk_jh9ezlkkcIV3gPAzTfsJVY_erqx5bHSXHBSgEpbFpQiFh/edit` を開く
2. ⚙ プロジェクトの設定 → 「スクリプト プロパティ」
3. `CHANNEL_ACCESS_TOKEN` の値を **新トークン**に更新（1Passwordからコピペ）
4. `OWNER_USER_ID` が入っているか確認（無ければ社長がLINEで「yawaragi社内」にメッセージ→自動登録される）
5. エディタで `testSend` を実行 → 社長のLINEにテスト通知が届けば **①完了**

### ② yawaragiボード（1pJN4vj）
1. 板GASを開く → スクリプト プロパティ
2. `LINE_TOKEN` = 新トークン、`OWNER_USER_ID` = ①からコピーした値、を設定
3. **コード反映**：repo `d4570cc`（getProperty版）を **clasp push**
   - `cd gas/yawaragi-board && clasp push`（※本番書き込み＝社長の手で）
   - push前に `clasp pull` で差分確認（本番に別の手編集が無いか）
4. 板の通知系機能でテスト（欠席通知等）→ 届けば **②完了**

### ③ インク管理
1. インク管理GASを開く → スクリプト プロパティに `LINE_TOKEN` / `OWNER_USER_ID` を設定
2. コードが `getProperty` 版か確認（平文が残っていれば getProperty へ差し替え）
3. インク残量通知でテスト → **③完了**

### ④ 複製 LINE通知（1_n-UuWw）の始末
- webhook未設定の**死んだ複製**で、平文の**旧（無効）トークン**を保持。
- LINE公式アカウントのwebhook URLが `1Pyoag` を指していることを確認のうえ、
  **このプロジェクトは削除**（または中身を空に）。放置は平文が残る温床。

---

## 完了チェックリスト
- [ ] ① 1Pyoag：`CHANNEL_ACCESS_TOKEN` 更新・`testSend` OK
- [ ] ② 板：プロパティ設定＋`clasp push`＋通知テスト OK
- [ ] ③ インク：プロパティ設定＋getProperty確認＋テスト OK
- [ ] ④ 1_n-UuWw：削除（または空化）
- [ ] repo `d4570cc` を `git push origin master`（社長判断）
- [ ] GitGuardian：4月インシデントを Reopen＋旧トークンRevoke登録・通知先確認（別途）

## やってはいけない
- 新トークンをコード・チャット・コミットに直書きしない（Script Properties のみ）。
- `clasp push` を pull 差分未確認で実行しない（本番の手編集を消す恐れ）。
