{
  "name": "yawaragi-apps-tests",
  "version": "0.0.0",
  "private": true,
  "description": "dev-only test harness for yawaragi-apps (product code untouched)",
  "scripts": {
    "test": "node scripts/run-all-tests.js"
  },
  "devDependencies": {
    "jsdom": "^24"
  }
}
